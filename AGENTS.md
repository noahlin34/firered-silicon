# AGENTS.md — Pokémon FireRed Native Apple Silicon Port

## 1. Project Overview
This repository is based on **pokefirered** (the pret decompilation of *Pokémon FireRed* for the Game Boy Advance).
- **Core Architecture:** 32-bit GBA C engine originally targeting the ARM7TDMI processor.
- **Primary Mission:** Port the game to run **natively as a 64-bit Mach-O ARM64 executable on macOS (Apple Silicon)** using **SDL2**, without requiring an external GBA emulator or GBA cross-compilation toolchains.

---

## 2. Key Technical Decisions & Constraints

### The 64-Bit Apple Silicon Reality
1. **No 32-bit binaries:** macOS on Apple Silicon (arm64) does not support 32-bit binaries or 32-bit ARM assembly (`armv4t`/`arm7tdmi`). Any inline assembly or `.s` files targeting GBA registers/instructions must be guarded with `#ifndef PORTABLE` or replaced with portable C.
2. **Memory Virtualization:** GBA physical addresses (`0x04000000` I/O registers, `0x05000000` Palette RAM, `0x06000000` VRAM, `0x07000000` OAM, `0x0E000000` Flash) cause segfaults if dereferenced directly on macOS. They are abstracted via `#ifdef PORTABLE` to point to host-allocated global buffers (`REG_BASE`, `PLTT_`, `VRAM_`, `OAM_`, `FLASH_BASE_`) in `src/platform/system.c`.
3. **No DMA pointer truncation:** GBA register-based DMA writes (`DmaSet`) cast source and destination addresses to 32-bit integers. On 64-bit systems, this truncates 64-bit pointers. In `include/gba/macro.h`, `DMA_COPY` and `DMA_FILL` are implemented as synchronous memory copy/fill functions under `PORTABLE`.
4. **Software GBA PPU:** The GBA engine does not write to a linear framebuffer; it writes tiles, tilemaps, and OAM structures. A software PPU (`src/platform/ppu.c`) composites Mode 0/1 background layers and 128 OAM sprites into a 240×160 15-bit BGR555 texture displayed via SDL2 at the GBA's approximately 59.7275 FPS.

---

## 3. Current Implementation Status

### Completed Milestones
- [x] **Native Asset Compilers:** `tools/gbagfx`, `tools/scaninc`, `tools/preproc`, etc. compile natively for ARM64 using Apple Clang and Homebrew `libpng`/`pkgconf`.
- [x] **Hardware Abstraction Layer (HAL):**
  - `include/gba/defines.h`: Host buffer mappings for `PLTT`, `VRAM`, `OAM`.
  - `include/gba/io_reg.h`: Array-backed `REG_BASE`.
  - `include/gba/macro.h`: 64-bit safe synchronous DMA macros.
  - `include/gba/flash_internal.h`: Re-routed `FLASH_BASE_` buffer.
- [x] **Portable GBA BIOS (`src/platform/bios.c`):**
  - `CpuSet` and `CpuFastSet` (16-bit and 32-bit memory fill and copy)
  - `LZ77UnCompWram` and `LZ77UnCompVram` (GBA LZ77 decompression)
  - `RLUnCompWram` and `RLUnCompVram` (RLE decompression)
  - `Div`, `Sqrt`, `ArcTan2`
  - `BgAffineSet`, `ObjAffineSet`
  - `RegisterRamReset`, `VBlankIntrWait`
- [x] **Software PPU Compositor (`src/platform/ppu.c`, `include/platform/ppu.h`):**
  - Scanline compositor for Mode 0 text backgrounds ($32\times32$, $64\times32$, $32\times64$, $64\times64$ tilemaps with 4bpp/8bpp tiles, flipping, and scrolling).
  - 128 OAM sprite compositor with transparency, priority sorting, and 4bpp/8bpp palettes.
  - **Affine (rotated/scaled) OAM sprites.** The sprite path computes an inverse affine transform per pixel from `gOamMatrices[oam.matrixNum]`, so the Poké Ball spins while the player throws it and the ball send-out's battler scale/shrink actually samples. Double-size (`affineMode >> 1 & 1`) sprites map their tile block against the *undoubled* object dimensions while covering a doubled bounding box, matching hardware. Before this the matrices were computed but never read — the ball flew without spinning.
  - **Display windows (WIN0/WIN1/OBJWIN).** `ComputeWindowMask()` classifies each pixel by region (priority WIN0 > WIN1 > OBJWIN > outside) and derives the region's effective layer enables from `REG_WININ`/`REG_WINOUT`. Masking is active only while a window is switched on in `REG_DISPCNT`; otherwise the ordinary layer enables apply everywhere. The window's colour-effect bit gates `REG_BLDY`/blend per pixel, and an OBJ-window sprite is not drawn — its opaque pixels only mark the region. This is what the OPTION menu's cursor-row highlight and the battle-transition wipes are built from.
  - **Per-scanline HBlank DMA.** `DmaSet` records HBlank-timed channels (`PortableDmaSet`, `src/platform/system.c`) and `Platform_RunHBlankDma()` replays one transfer per rendered scanline, so per-scanline effects that feed `REG_WIN0H`/`REG_BLDY`/`REG_BGxHOFS` through DMA work. Immediate (`DMA_START_NOW`) transfers still run inline.
  - Alpha blending (`REG_BLDCNT`, `REG_BLDALPHA`) and brightness increase/decrease (`REG_BLDY`).
  - Mode 1 affine BG2 compositor renders Oak's scrolling/zooming backdrop and trainer portraits; verified through Oak and the selected player portrait.
- [x] **SDL2 Platform Host Runner (`src/platform/sdl2.c`, `include/platform/platform.h`):**
  - Resizable 3× scaled window ($720\times480$) with high-DPI support.
  - Keyboard mapping (WASD/Arrows, Z/X/J/K, Enter, Tab/Backspace) mapped to active-low `REG_KEYINPUT`.
  - Screenshot capture functionality (`Platform_SaveScreenshot`).
  - Top-left FPS overlay (development aid): `UpdateFpsCounter()` samples presented frames over 0.5 s windows and `DrawFpsOverlay()` draws a 5×7 bitmap font in the renderer's logical 240×160 space (translucent backing bar, then white glyph pixels as 1×1 rects). It draws after the GBA texture and never touches `sFramebuffer`, so `Platform_SaveScreenshot` captures stay byte-identical and overlay-free. Verified live: window shows `FPS 60` (paced 59.7275 Hz) in the bedroom.
  - The overlay is entirely behind `#ifdef FPS_OVERLAY` (state, glyph table, `UpdateFpsCounter()`/`DrawFpsOverlay()` calls). **Production removal: build with `FPS_OVERLAY=0`** — see §5. Nothing to delete from `src/platform/sdl2.c`; leave the guard in place so it stays available for dev builds.
  - Display-independent GBA frame pacing: `WaitForFrameDeadline()` in `src/platform/sdl2.c` runs before rendering in `Platform_RenderAndPresent()`. Interactive play, boot automation, and the standalone PPU test share this clock.
- [x] **CPU Game Engine Linked (`src/main.c` & Subsystems):**
  - `src/main.c` has its 32-bit assembly guarded and `WaitForVBlank()` hooked into the platform tick (`Platform_UpdateInput()`, `VBlankIntr()`, `Platform_RenderAndPresent()`).
  - Core subsystems linked: `gpu_regs.c`, `palette.c`, `task.c`, `sprite.c`, `malloc.c`, `scanline_effect.c`, `dma3_manager.c`, `bg.c`, `random.c`, `trig.c`, `decompress.c`, `blend_palette.c`.
  - Hardware stubs for sound (M4A) and link communications in `src/platform/stubs.c`.
- [x] **Title Screen Scene (`src/title_screen.c`):**
  - Charizard box art sprite, FireRed title logo, copyright bar, "PRESS START", and animated flame particles fully rendered and animating at the GBA frame rate in SDL2.
  - Native `INCBIN` asset pipeline using `tools/gbagfx` and `tools/preproc/preproc`.
- [x] **Main Menu Scene (`src/main_menu.c`):**
  - Pressing START/A on the Title Screen transitions through `SetTitleScreenScene_Cry` into `CB2_InitMainMenu`.
  - Renders "CONTINUE" (PLAYER / TIME / BADGES stats) and "NEW GAME" windows with user-frame tiles.
- [x] **Oak's Speech Scene (`src/oak_speech.c`):**
  - Pressing DOWN+A on the Main Menu selects NEW GAME, transitioning through `StartNewGameScene` → `Task_NewGameScene` → Controls Guide (3 pages) → Pikachu intro → Oak's speech.
  - Professor Oak's speech text renders with dialog windows; controls guide pages and Pikachu intro fully working.
  - Battle engine now linked (see the Battle Engine milestone and §6 item 7). `src/platform/battle_engine_stubs.c` retains only the non-battle scene substitutes and loud-failure tripwires, and `src/platform/battle_peripheral_stubs.c` holds the peripherals a wild battle never reaches.
  - Asset pipeline: all `graphics/pokemon/*`, `graphics/trainers/*`, `graphics/items/*`, `graphics/interface/*`, `graphics/battle_*` INCBIN assets regenerated via `gbagfx`; split sprite sheets (`_0.png.._N.png`) must be combined vertically with width padding to 64px before 4bpp conversion.
  - Mode 1 BG0/BG1 text and affine BG2 rendering are working. Oak, Nidoran F, and the selected player portrait are visually verified in boot-test captures.
- [x] **Player Naming Flow (`src/naming_screen.c`):**
  - Boot automation reaches gender selection, enters the player naming screen, accepts a player name, and returns to Oak's speech without crashing.
  - Naming-screen teardown now disables VBlank before freeing `sNamingScreen`; `CB2_NamingScreen` stops the same frame when its task frees that state.
  - Naming keyboard letters and punctuation render from the real charmap-encoded `src/keyboard_text.c`; the blank battle-stub definitions were removed.
- [x] **Rival Naming & Oak's Speech Exit Flow (`src/naming_screen.c`, `src/oak_speech.c`):**
  - Oak's post-player-name dialogue enters the rival naming screen, inputs the rival name, and returns to Oak's speech.
  - Player and rival naming icons render cleanly using fully initialized local sprite templates.
  - Oak confirms the rival name, delivers his farewell dialogue, and triggers the exit animation (`Task_OakSpeech_ShrinkPlayerPic` affine Mode 1 BG2 shrink and fade-to-black).
  - Oak's speech resources are freed and cleanly hand off execution to `CB2_NewGame` (`[Engine] Successfully completed Oak's speech and reached CB2_NewGame!`).
- [x] **Overworld Map Engine & Player Spawn (`src/overworld.c`, `src/new_game.c`, `src/fieldmap.c`, `src/maps.c`):**
  - Replaced `CB2_NewGame` stub with real engine initialization (`NewGameInitData()`).
  - Implemented 64-bit safe C map data generator (`tools/gen_map_data.py`) producing native 64-bit struct layouts (`src/data/layouts_data.h`) and map headers (`src/data/maps_data.h`), avoiding 32-bit GBA assembly pointer truncation.
  - Linked real field engine subsystems: `fieldmap.c`, `field_camera.c`, `field_player_avatar.c`, `event_object_movement.c`, `field_control_avatar.c`, `field_tasks.c`, `field_weather.c`, `field_weather_effects.c`, `field_fadetransition.c`, `metatile_behavior.c`, `bike.c`, `wild_encounter.c`, `script.c`, `scrcmd.c`, `tilesets.c`, `field_door.c`, `field_effect.c`, `maps.c`.
  - Converted all 67 game tilesets to `4bpp.lz` and 1,072 tileset palettes to `.gbapal`.
  - Fixed saveblock pointer ASLR under `PORTABLE` in `src/load_save.c`.
  - Boot automation at frame 9200 successfully executes `CB2_NewGame`, initializes save data, warps to `MAP_PALLET_TOWN_PLAYERS_HOUSE_2F`, draws the complete bedroom map view (bed, rug, TV/NES, PC, desk, stairs), spawns the player avatar facing North, and enters `CB2_Overworld` running at the GBA frame rate in SDL2.
- [x] **Dev Save Fast Boot (`--skip-intro`, `--post-rival`, `src/platform/dev_boot.c`):**
  - `Platform_DevBootNewGame()` runs the title screen's save initialization (`SeedRngAndSetTrainerId` → `SetSaveBlocksPointers` → `ResetMenuAndMonGlobals` → `Save_ResetSaveCounters` → `LoadGameSave` → `Sav2_ClearSetDefault`) on a fresh save, writes the naming screens' first choices (player `RED` male, rival `GREEN`), and hands off to `CB2_NewGame` — the same entry point Oak's farewell dialogue uses. Skips title screen, main menu, controls guide, Oak's speech, and both naming screens.
  - `--post-rival` starts from that same fresh save but hands off to `CB2_DevBootPostRival` instead: it calls `CB2_NewGame` (which performs the whole bedroom map load), then `Platform_DevBootApplyStoryProgress()` writes the state `PalletTown_ProfessorOaksLab`'s starter scripts leave behind, then warps into Oak's Lab at `(6,8)` via the same `SetWarpDestination`/`WarpIntoMap`/`CB2_LoadMap` hand-off `ScrCmd_warp` uses. The bedroom frame is never rendered — `CB2_LoadMap` clears the screen and the lab fades in.
  - `Platform_DevBootApplyStoryProgress()` writes exactly the values a real playthrough produces, with each line annotated by the script statement it mirrors: `ScriptGiveMon(SPECIES_BULBASAUR, 5)` + `HealPlayerParty()` (the player's starter), `FLAG_SYS_POKEMON_GET`, `FLAG_PALLET_LADY_NOT_BLOCKING_SIGN`, `FLAG_VISITED_OAKS_LAB`, `FLAG_HIDE_OAK_IN_HIS_LAB` cleared / `FLAG_HIDE_OAK_IN_PALLET_TOWN` set (Oak is still in the lab), `FLAG_HIDE_RIVAL_IN_LAB`, `FLAG_HIDE_BULBASAUR_BALL` + `FLAG_HIDE_CHARMANDER_BALL` (starter and counter-pick removed; Squirtle stays), `FLAG_BEAT_RIVAL_IN_OAKS_LAB`, `SetTrainerFlag(TRAINER_RIVAL_OAKS_LAB_CHARMANDER)`, `VAR_STARTER_MON = 0`, `VAR_MAP_SCENE_PALLET_TOWN_OAK = 1`, and `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB = 4` (`EndRivalBattle`'s value). Keep it in sync if the starter scene changes.
  - `--post-rival` implies `gPlatformSkipIntro`, so both dev boots share the save setup. The intro key sequence is gated on `!gPlatformSkipIntro`; the bedroom NES replay (`--skip-intro` UP at 60–65, A at 90–95) is additionally gated on `!gPlatformSkipStory`, because that interaction does not exist in the lab.
  - Names come from the charmap-encoded `gNameChoice_*` tables (`src/platform/new_game_intro_text.c`), never plain ASCII, so the save data renders and terminates correctly.
  - Hooked in `AgbMain()` after `SetDefaultFontsPointer()` and guarded by `#ifdef PORTABLE`; the flags are parsed in `src/platform/main.c` and declared in `include/platform/platform.h`.
  - Verified: `./firered-native --post-rival --boot-test 60` lands in `MAP_PALLET_TOWN_PROFESSOR_OAKS_LAB` at `(6,8)` with the party holding one Lv5 Bulbasaur (species 1, HP 19/19), the lab scene var at 4, `FLAG_BEAT_RIVAL_IN_OAKS_LAB` set, controls unlocked, and the correct object events spawned (Oak, three aides, both dex units; rival and the Bulbasaur/Charmander balls gone, Squirtle ball present). Holding DOWN walks straight out the door into Pallet Town, and continuing west moves freely across town — no locked field, no lingering trigger. A PPU capture shows the lab interior with the player at the exit mat.
- [x] **Pallet Town Exterior, Front-Door Warp & Route Connections (`src/field_door.c`, `src/field_effect.c`, `src/wild_encounter.c`, `tools/gen_map_data.py`):**
  - Walking out of the front door works in both directions (1F ↔ Pallet Town) with the real door open/close animation, and Pallet Town renders its tilesets, NPCs, signs, and weather.
  - Outdoor maps now carry generated `struct MapConnections`, so the neighbouring map fills `VMap` at the border and the player can walk across it (Pallet Town ↔ Route 1 / Route 21).
  - Fixing this exposed four defects on the way out — door-animation pointer truncation, coord-event dummy-script livelock, field-effect stub table overrun, and a wild-encounter lock with no battle engine. See §3 fixes 26–29.
- [x] **Professor Oak's Lab Interactions (`tools/gen_map_data.py`, `src/data/maps_data.h`, `src/platform/main.c`):**
  - All ten object events and four background events in `MAP_PALLET_TOWN_PROFESSOR_OAKS_LAB` were `sDummyScript`; pressing A on an aide, the rival, a starter ball, a dex unit, a PC or a sign did nothing.
  - The three aides, the rival, the two dex units, the two computer terminals, the two signs, the three starter balls and Oak himself now run their real scripts (see §6 items 3 and 5).
  - `tools/gen_map_data.py` gained `copyvar`, `waitstate`, `removeobject`, `showmonpic`, `hidemonpic`, `givemon`, `bufferspeciesname`, `MSGBOX_YESNO` and the `YES`/`NO` constants; it now treats text defined inline in `data/event_scripts.s` as a text source and no longer reads an `.include` directive as a script command.
  - Verified with a temporary warp-stand-turn-press probe: 11/11 reachable targets opened their dialogue (`gStringVar4` charmap-correct, field locked while the box waited), and a PPU capture shows the aide's "I study POKéMON as PROF. OAK's AIDE." box.
- [x] **Professor Oak's Pallet Town Interception Cutscene (`src/data/maps_data.h`, `tools/gen_map_data.py`, `src/platform/overworld_stubs.c`):**
  - Stepping on the northern exit of Pallet Town (`(12,1)` or `(13,1)`) when `VAR_MAP_SCENE_PALLET_TOWN_OAK == 0` triggers Oak's interception cutscene (`PalletTown_EventScript_OakTriggerLeft`/`Right`): Oak shouts ("OAK: Hey! Wait! Don't go out!"), player turns around with the exclamation emote, Oak spawns and walks up from below, warns the player about tall grass, leads Red all the way down across Pallet Town to his lab, opens the lab door (`opendoor 16, 13`), both enter and turn invisible, the door closes (`closedoor 16, 13`), flags/vars are updated, and the game warps the player directly inside Oak's Lab at `(6,12)` with controls unlocked and the lab scene active.
  - `tools/gen_map_data.py` gained support for assembler `.macro ... .endm` parsing and expansion in map script sources (expanding `walk_to_lab` without polluting adjacent labels) and native bytecode emission for `playbgm`, `addobject`, `opendoor`, `closedoor`, `waitdooranim`, `warp`, `setworldmapflag`, `setobjectxyperm`, `setobjectmovementtype`, `specialvar`, and `buffernumberstring`.
  - `src/platform/overworld_stubs.c` gained `NativeSpecial_UpdatePickStateFromSpecialVar8005` at index 372 in `gSpecials[]`.
  - Verified end-to-end via an 1,800-frame test run: Oak intercept, full walk down Pallet Town, lab door open/close animation, and successful warp into `PalletTown_ProfessorOaksLab` at `(6,12)` with `script_active=0`, `VAR_MAP_SCENE_PALLET_TOWN_OAK=1`, `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB=1`, and the lab interior fully rendered.
- [x] **Oak's Lab Starter Scene (`src/data/maps_data.h`, `tools/gen_map_data.py`, `src/script.c`, `src/script_pokemon_util.c`, `Makefile.native`):**
  - The lab's `mapScripts` header (`map_script`/`map_script_2` on `ON_TRANSITION`, `ON_WARP_INTO_MAP_TABLE`, `ON_FRAME`) is compiled natively, so warping in after Oak's interception cutscene runs `ReadyOakForStarterScene`, `ReadyPlayerForStarterScene` and `ChooseStarterScene` automatically. Oak walks in, the player and rival are positioned, the long speech plays, and `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` becomes 2 with controls unlocked.
  - The three balls then run their real `ConfirmStarterChoice`/`ChoseStarter` scripts: species picture (`ScriptMenu_ShowPokemonPic`), YES/NO confirmation, `removeobject VAR_LAST_TALKED`, `FLAG_SYS_POKEMON_GET`, real `ScriptGiveMon`, obtain fanfare, optional nickname screen (`special ChangePokemonNickname`), and the rival taking the counter-pick before the scene var reaches 3. Oak is interactable throughout.
  - `MapHeaderGetScriptTable`/`MapHeaderCheckScriptTable` resolve map-script operands through `gNativeScriptPtrs` under `PORTABLE` (fix #23); `gSpecials` gained index-aligned `ChangePokemonNickname` (158) and the quest-log wrappers.
  - Verified end-to-end on the real user path (see §6 item 5): cutscene → lab → auto starter scene → Bulbasaur received (`species=1`, `VAR_STARTER_MON=0`) → rival takes Charmander → scene var 3, with PPU captures of the confirmation, "really quite energetic!", "GREEN received the CHARMANDER" and Oak's A-press dialogue boxes.
- [x] **Oak's Parcel Chain & Pokédex Hand-off (`src/data/maps_data.h`, `tools/gen_map_data.py`, `src/scrcmd.c`, `Makefile.native`):**
  - The Viridian City Mart's `mapScripts` header is compiled natively (`ON_LOAD` → `HideQuestionnaire`, `ON_FRAME_TABLE` → `ParcelScene` when `VAR_MAP_SCENE_VIRIDIAN_CITY_MART == 0`), so walking into the Mart hands the player `ITEM_OAKS_PARCEL` and advances Oak's lab scene to 5.
  - Oak's parcel branch (`ReceiveDexScene`) is reachable and real: the parcel is removed, the rival walks in, the Pokédex is obtained (`FLAG_SYS_POKEDEX_GET`, `SetUnlockedPokedexFlags`), the desk dex units disappear, five Poké Balls are added, and the lab scene settles at 6.
  - Route 1 and Viridian City compile too (signs, the free-Potion clerk, the city `ON_TRANSITION`), so the walk to the Mart is populated. The tutorial old man's `TutorialTriggerLeft`/`Right` coord events still emit NULL, but no longer because the battle engine is missing: their `ViridianCity_EventScript_DoTutorialBattle` calls the `StartOldManTutorialBattle` special, which is still a stub (the Poké Dude/catching-demo path).
  - `tools/gen_map_data.py` gained `pokemart`, `setmetatile`, `checkitemspace`, `bufferitemname` and `.align`, and `TextRegistry.reference_global()` now emits engine-referenced text labels (scanned from `extern const u8` declarations in linked `src/**.c`) so `src/prof_pc.c`'s rating messages resolve.
  - `src/prof_pc.c` and `src/save_location.c` are linked; `src/scrcmd.c`'s shop commands degrade loudly instead of locking the field (fix #37).
  - Verified end-to-end with a temporary probe: parcel obtained in the Mart, handed to Oak, Pokédex + five Poké Balls granted, scene vars at 6/2; PPU captures show Oak's dex-scene boxes. See §6 item 6.

> **Note on Intro Sequence:** The opening intro sequence (`src/intro.c` — Copyright screen, GameFreak star shooting animation, and Nidorino vs. Gengar battle) is temporarily bypassed in `src/main.c` (`InitMainCallbacks()` boots directly to `CB2_InitTitleScreen`). This was done deliberately to expedite reaching gameplay, and the intro cinematic will be linked back in at a later point in time.

- [x] **Oak's Lab Exit Rival Battle (`tools/gen_map_data.py`, `tools/gen_battle_data.py`, `src/battle_setup.c`, `src/battle_peripheral_stubs.c`):**
  - Leaving the lab after choosing a starter now stops the player at `(5..7, 8)` and starts the first rival battle. Previously the player walked straight over those tiles: the scene-3 coord events were emitted with `.script = NULL` (their scripts were not in `NATIVE_SCRIPT_ROOTS`), so the automatic proximity trigger was simply walkable.
  - The real battle chain runs: the trigger sets `VAR_TEMP_2` to the player's column, `RivalBattle` plays the encounter music and turns both trainers, the rival walks along one of nine approach paths, and `trainerbattle_earlyrival` starts `TRAINER_BATTLE_EARLY_RIVAL` with `RIVAL_BATTLE_TUTORIAL` against `TRAINER_RIVAL_OAKS_LAB_{BULBASAUR,CHARMANDER,SQUIRTLE}` (whichever counters the player's starter). `EndRivalBattle` heals the party, walks the rival out, removes him, sets the lab scene var to 4 and sets `FLAG_BEAT_RIVAL_IN_OAKS_LAB`.
  - Three generator/engine fixes were needed (see §3 fixes #46-#48). `tools/gen_map_data.py` gained `trainerbattle_earlyrival`; `tools/gen_battle_data.py` had three macro-expander defects that desynchronised **every** animation script and hung any move animation on `Cmd_nop`; and `EventScript_DoNoIntroTrainerBattle` — the script `BattleSetup_ConfigureTrainerBattle` jumps into — was a `{0x02}` stub.
  - Verified end-to-end on the real user path (Pallet Town → Oak's interception → lab → starter → walk down to `(7,8)`): the battle starts at frame 3840 with `battleFlags=0x1c` (`IS_MASTER|TRAINER|FIRST_BATTLE`), runs real turns (HP 20/20 → 12/20, opponent faints, exp awarded, player's mon Lv5 → Lv6), ends with `outcome=1`, and returns to the field with the lab scene at 4 and controls unlocked. A loss path (`outcome=2`) also completes and returns. PPU captures show the battle screen, both healthboxes, the damage, and the level-up.
  - `TestOverworldInteractions()` asserts the three triggers are wired to their real scripts through the pointer table and dispatch on `VAR_STARTER_MON`, so a reintroduced `NULL` fails loudly at startup.

- [x] **Field Effects Ported (`tools/gen_field_effect_data.py`, `src/data/field_effects/*`, `src/field_effect_helpers.c`, `src/field_effect.c`):**
  - **Walking through tall grass now plays the rustle.** `data/field_effect_scripts.s` cannot be assembled for the host, so `tools/gen_field_effect_data.py` compiles it to native C: pret's opcodes and operand widths byte-for-byte, with every 4-byte address operand stored as a u32 index into the aligned `gNativeFieldEffectPtrs[]` (`FieldEffectScript_ReadWord()` resolves it, and now returns `uintptr_t` — the old `u32` discarded the top half of every host pointer).
  - **Which effects are emitted is derived, not hand-listed.** The generator reads `ENGINE_SRCS`/`PREPROC_SRCS` from `Makefile.native`, walks the `#include` closure, and requires a real definition (a header's `extern` is not one); unlinked natives leave the effect NULL, which is exactly the old all-NULL stub's behaviour. 57 of 70 scripts are emitted, including the whole grass family (`TallGrass`, `ShortGrass`, `LongGrass`, `JumpTallGrass`, `JumpLongGrass`), `Dust`, `Ripple`, `Splash`/`Jump(Big|Small)Splash`, `SandFootprints`/`DeepSandFootprints`, `BikeTireTracks`, `FeetInFlowingWater`, `HotSpringsWater`, `Shadow`, `Ash`, `SandPile`, `Bubbles`, `Sparkle` and the Pokécenter/Hall-of-Fame/fly-out screens.
  - **Runtime:** `src/field_effect_helpers.c` is linked and the 26 now-duplicate stub functions in `src/platform/overworld_stubs.c` are deleted (fix #8), as is its all-NULL `gFieldEffectScriptPointers` — the generated `src/data/field_effects/ptr_table.c` is the real definition. The object templates, palettes, OAM bases and pic assets were already linked via `event_object_movement.c`, so no new asset rules were needed.
  - **Verified** with a temporary probe: `GroundEffect_SpawnOnTallGrass()` (the function `MB_TALL_GRASS` dispatch calls) put `FLDEFF_TALL_GRASS` in the active list and created a sprite whose VRAM tiles **byte-match `graphics/field_effects/pics/tall_grass.4bpp` frame 0** (found at OBJ VRAM tile 20); a capture inside the rustle's ~52-frame lifetime differs from one after it by exactly a 16px-wide pixel span at the player's feet. Route 1 was reached with `DoWarp()` + `ResetInitialPlayerAvatarState()` so the map, camera offset and player object event were all consistent. `TestOverworldInteractions()` now asserts the tall-grass script decodes to `loadfadedpal_callnative <palette>, <native>, end` with operands resolving to `gSpritePalette_GeneralFieldEffect1` and `FldEff_TallGrass`, that the other grass-family effects are present, and that an unported effect is still NULL. The probe and its `--probe-fldeff` flag were removed afterwards.

- [x] **START Menu, BAG, POKéMON & OPTION Screens (`src/start_menu.c`, `src/item_menu.c`, `src/bag.c`, `src/option_menu.c`, `src/party_menu.c`):**
  - Pressing START now opens the real menu, and BAG, POKéMON and OPTION all open and render correctly. `src/start_menu.c`, `src/item_menu.c`, `src/bag.c`, `src/menu_indicators.c`, `src/item_menu_icons.c`, `src/option_menu.c` and `src/save_menu_util.c` are linked (the item/start-menu sources join `PREPROC_SRCS` for their `_()` strings and `INCBIN` assets), with new asset rules for `bag_window_pal`, `select_button`, `red_arrow*`, `list.bin` and the menu icon sheets.
  - **The blocker was a 64-bit pointer split across task slots** (fix #50): `SetTaskFuncWithFollowupFunc` wrote the follow-up `TaskFunc` through `data[NUM_TASK_DATA - 2]`/`[+1]`, exactly 4 bytes, so every screen that uses the follow-up pattern — the START menu, the BAG's pocket switch and the party menu — jumped into unmapped memory the frame it switched tasks. The pointer now lives in a `sTaskFollowupFuncs[NUM_TASKS]` side table keyed by `taskId`, so no task's `data` layout shifts.
  - `NATIVE_SCRIPT_ROOTS` gained `EventScript_BagItemCanBeRegistered`, the message the BAG runs when an item is registered to SELECT; without it that path jumped into a NULL. `src/platform/battle_engine_stubs.c` and `src/platform/battle_peripheral_stubs.c` dropped the placeholder start-menu/item-menu symbols now provided by the real sources.
  - **Verified** with a temporary probe (since removed): START opened the menu; the BAG listed `POTION x3`/`ANTIDOTE x2`/`POKé BALL x5`/`TM01` with correct descriptions and RIGHT switched pockets (`gBagMenuState.pocket` 0 → 1); the POKéMON menu drew BULBASAUR Lv5 with its HP bar and "Choose POKéMON or CANCEL."; the OPTION menu drew TEXT SPEED / BATTLE SCENE / BATTLE STYLE / SOUND / BUTTON MODE / FRAME / CANCEL. No crash; the field resumes with `locked=0`.

- [x] **Battle Engine Linked (`tools/gen_battle_data.py`, `src/data/battle/*`, `src/battle_script_commands.c` + 60 more):**
  - The real GBA battle engine is compiled and linked. `data/battle_scripts_1.s` (4,382 lines), `_2.s`, `battle_anim_scripts.s` (11,103) and `battle_ai_scripts.s` cannot be assembled for the host, so `tools/gen_battle_data.py` compiles them to native C byte arrays with pret's exact layout.
  - **Pointer operands are index-based (fix #38).** Every 4-byte operand slot classified as a pointer holds a u32 index into the aligned `gNativeBattlePtrs[]` table; `T1_READ_PTR`/`T2_READ_PTR` in `include/global.h` resolve that index (declaration in `global.h`, definition in `src/data/battle/ptr_table.c`). 1,659 deduplicated entries across 1,800 scripts. 4-byte *numeric* operands keep their literal value and stay on `T1_READ_32`/`T2_READ_32`.
  - **The slot classification is derived mechanically from the C readers, not hand-maintained.** `derive_opcode_readers()` walks each `Cmd_*` body (the dispatch table is authoritative for opcode→handler, which matters where a macro name differs from its handler, e.g. `tryfaintmon_spikes` encodes 0x19 handled by `Cmd_tryfaintmon`), tracking the script pointer as a *set* of possible cursors so battle (fixed `ptr + N`) and animation/AI (advance-then-read) dialects both resolve. A slot with no reader is a hard generator error. Cross-checked against an independent audit: **265 slot rows, 0 disagreements**.
  - **Every battle source compiles** (49 files) and 60 files are linked, including `battle_util2.c` (`AllocateBattleResources` — without it `gBattleResources->battleHistory` is NULL and the AI setup crashes), `berry.c` (`GetBerryInfo`), `pokemon_special_anim.c`, `safari_zone.c`, `battle_tower.c`, `battle_intro.c`, `party_menu.c` and all 25 `battle_anim_*.c`.
  - **Assets:** 81 battle `INCBIN` targets + 72 more for the newly linked scenes, generated by `tools/battle_assets.mk`, `tools/battle_extra_assets.mk` and `tools/anim_sprite_assets.mk` (269 animation sprite sheets, 5 of them split families needing vertical combination — see fix #9). `anim_sprite_assets.mk` closes a pre-existing gap where those 269 `.4bpp.lz` files had no build rule, so a clean clone could not build `graphics.o` at all.
  - **Encounters are enabled:** the `PORTABLE` early-out in `StandardWildEncounter()` is removed and the `StartWildBattle()` stub deleted, so grass encounters reach the real battle engine.
  - **Generator battle commands:** `tools/gen_map_data.py` now emits `trainerbattle`, `dotrainerbattle`, `setwildbattle`, `dowildbattle`, `gotopostbattlescript` and `gotobeatenscript`.
  - **Verified:** the engine boots, `CB2_InitBattle` → `BattleMainCB2` runs with `BATTLE_TYPE_WILD`, and the intro state machine advances the full sequence `BeginBattleIntro → GetMonsData → PrepareBackgroundSlide → DrawTrainersOrMonsSprites → DrawPartySummaryScreens → PrintWildMonAttacked → PrintPlayerSendsOut → PlayerSendsOutMonAnimation → TryDoEventsBeforeFirstTurn`. A PPU capture shows the battle screen: player healthbox with HP bar, battle terrain, and the "Wild … appeared!" message box. Clean boot-test passes with every smoke test green.
  - **Bugs found while bringing this up** (fixes #41-#45): three 64-bit pointer truncations - `GetWindowAttribute(WINDOW_TILE_DATA)` returning a pointer through `u32`, `TrainerBattleLoadArgs` writing pointers via `SetU32`, and `pokeball.c` splitting the mon pointer across two 16-bit task slots; a sprite-callback truncation in `battle_anim_mons.c`'s `StoreSpriteCallbackInData6`, which splits a `SpriteCallback` across `data[6..7]` and therefore required widening `struct Sprite.data` to `s32[8]` under `PORTABLE` (#44); and a VBlank callback that outlived the state it read in `battle_transition.c` (#42).
  - **With those fixed** the intro state machine runs to completion - `... -> PrintPlayerSendsOut -> PlayerSendsOutMonAnimation -> TryDoEventsBeforeFirstTurn` - the "Go! BULBASAUR!" send-out message renders, the player's mon leaves its ball, and the run is stable for 4,000 frames with no crash.
  - **Known gap:** the engine parks in `TryDoEventsBeforeFirstTurn` because `gBattleControllerExecFlags` stays set for battler 0. The pending controller command is `CONTROLLER_PRINTSTRING`, and the text printer sits in `RENDER_STATE_CLEAR` with `currentChar` already on `EOS`; that state waits on `TextPrinterWaitWithDownArrow()`, i.e. a fresh `JOY_NEW(A_BUTTON|B_BUTTON)` edge. This is **not** a port defect: the battle is genuinely waiting for a button press. A scripted run must supply a press/release cadence (a held key produces no edge) to advance each message box. Trainer battles compile in the generator but were not exercised.

### Critical 64-Bit Portability Fixes (Learned the Hard Way)
These bugs are subtle and WILL recur if new engine files are linked. Understand them before adding `ENGINE_SRCS`:

1. **GBA address-range checks break on host pointers.** Any engine code comparing a pointer against GBA constants (`IWRAM_END`, `EWRAM_START`, etc.) is wrong on 64-bit macOS — host pointers always compare "greater". Guard with `#ifdef PORTABLE` and check for `NULL` instead. Example: `IsTileMapOutsideWram` in `src/bg.c`.
2. **GameFreak string literals MUST go through `tools/preproc`.** The engine uses a custom charmap (`charmap.txt`), not ASCII. Strings are encoded bytes terminated by `EOS` (0xFF). If `_(x)` expands to `(x)` (plain ASCII), strings never terminate and `StringAppend`/text rendering walk off buffers and hang. All sources containing `_("...")` or `INCBIN_*` must be listed in `PREPROC_SRCS` in `Makefile.native` (piped through `clang -E | preproc | clang`), NOT plain `CFLAGS` compilation. Affected so far: `title_screen.c`, `sprite.c`, `text.c`, `text_window.c`, `text_window_graphics.c`, `strings.c`, `string_util.c`, `main_menu.c`, `new_menu_helpers.c`, `oak_speech.c`, `naming_screen.c`, `pokemon.c`, `battle_message.c`, `keyboard_text.c`, `util.c`, `battle_main.c`, `pokemon_icon.c`, `pokemon_storage_system_data.c`, `graphics.c`, `data.c`.
3. **DMA3 must be synchronous in PORTABLE mode.** `RequestDma3Copy`/`RequestDma3Fill` in `src/dma3_manager.c` perform instant `memcpy` and return immediately; `WaitDma3Request` always reports idle. The GBA queue-based path is guarded with `#else`.
4. **REG_VCOUNT must be 160 before `VBlankIntr()`.** `ProcessDma3Requests` and other engine code check `REG_VCOUNT > 224` to detect end-of-VBlank. `PPU_RenderFrame` leaves VCOUNT at 227; `src/main.c` `WaitForVBlank()` resets it to 160 each frame.
5. **`gFonts` must be initialized before any text is printed.** `SetDefaultFontsPointer()` now lives in the real `src/new_menu_helpers.c` (called via `InitTextBoxGfxAndPrinters`). If text prints with `gFonts == NULL`, `AddTextPrinter` silently no-ops or downstream code hangs.
6. **Save file status defaults to `SAVE_STATUS_EMPTY` (0)** in `src/platform/stubs.c` (`gSaveFileStatus`) so pressing START on the title screen starts a fresh game directly into character creation. It was previously hardcoded to 1 (`SAVE_STATUS_OK`) during early main menu porting to test the CONTINUE window, which caused interactive runs to select CONTINUE and hang on unlinked save-loading stubs.
7. **Software PPU video MODE 1.** Oak's Speech calls `InitBgsFromTemplates(1, ...)`: BG0/BG1 are text layers and BG2 is affine. `PPU_RenderScanline` has separate Mode 1 text and affine paths. Oak, the selected player portrait, and the rival naming icon are visually verified; the player-shrink transition remains to be checked after completing rival naming.
8. **Duplicate symbols when linking real engine files.** `src/platform/stubs.c` previously defined functions/data (`SetDefaultFontsPointer`, `DrawSpindaSpots`, `gMonFrontPicTable`, `gBattle_BG0_X`, etc.) now provided by linked engine files (`new_menu_helpers.c`, `pokemon.c`, `graphics.c`). When linking a real implementation, DELETE the stub version — never the reverse.
9. **Split sprite-sheet PNGs** (`name_0.png`, `name_1.png`, …) in `graphics/battle_anims/sprites/` must be combined into ONE png (vertically stacked, padded to width 64) before `gbagfx` 4bpp conversion; `gbagfx` cannot read them individually.
10. **`gbagfx` palette sources:** `INCBIN_U16("...gbapal")` sources are `.pal` (JASC) files, not PNGs. Convert `.pal → .gbapal → .gbapal.lz`.
11. **Naming-screen teardown spans the rest of the current frame.** `MainState_Exit` runs inside `RunTasks()`, changes the main callback, and frees `sNamingScreen`, but the old `CB2_NamingScreen` frame would otherwise continue into sprite animation and VBlank. Disable VBlank before freeing the state and return from `CB2_NamingScreen` immediately after `RunTasks()` when `sNamingScreen == NULL`.
12. **Stubbed sprite factories are unsafe for real scenes.** Returning 0 from `CreateObjectGraphicsSprite` is not a harmless no-op: callers mutate `gSprites[spriteId]`, corrupting whichever real sprite owns slot 0. Returning `MAX_SPRITES` is also unsafe when callers index without checking. Naming screens now create their icons from local, fully initialized templates; future callers must do the same or link the real object-event graphics path.
13. **`RegisterRamReset(RESET_REGS)` must restore affine identity matrices.** Clearing `REG_BASE` to zero collapses affine BG2/BG3 to one source pixel, hiding Oak and the player/rival portraits even though their tiles and maps are valid. After clearing registers, initialize `REG_BG2PA`, `REG_BG2PD`, `REG_BG3PA`, and `REG_BG3PD` to `0x100`.
14. **Real text tables must replace blank stubs completely.** The naming keyboard was blank because `battle_engine_stubs.c` supplied one-byte zero-filled `gText_NamingScreenKeyboard_*` arrays while `src/keyboard_text.c` was not linked. Add text-bearing sources to `PREPROC_SRCS` and delete every duplicate stub definition.
15. **Map headers, layouts, and script tables must use 64-bit host pointers.** GBA assembly `.4byte` pointers truncate 64-bit pointers on macOS ARM64. `tools/gen_map_data.py` compiles map layouts, headers, and event structures directly as native C structs in `src/maps.c`, where pointers are natively 64-bit (`sizeof(void *) == 8`).
16. **SaveBlock ASLR offset must be zero in PORTABLE mode.** GBA `SetSaveBlocksPointers` randomizes offsets up to 128 bytes assuming EWRAM padding. On macOS, save blocks are exact C struct buffers; adding an offset walks past buffer boundaries.
17. **Script command table (`gScriptCmdTable`) requires native function pointer sizing.** Script command dispatch indexes function pointers; on 64-bit architectures, this table must contain 8-byte pointers generated in C (`src/data/script_cmd_table.h`).
18. **`ForestMapPreviewScreenIsRunning` must return TRUE when idle.** In pret decompilation, this function returns whether the preview screen is *not* active. Returning FALSE keeps `FieldFadeTransitionBackgroundEffectIsFinished()` permanently false, preventing `Task_ExitNonDoor` from unfreezing objects and unlocking player controls upon warp entry.
19. **Map script headers must be terminated by 0x00.** Bytecode scripts terminate with `0x02` (`end`), but map header `.mapScripts` lists entries terminated by `0x00`. Using `0x02` causes `MapHeaderGetScriptTable` to overrun the array on frame-script checks.
20. **`src/menu.c` handles dialogue choice and cursor input.** Menu selection (`Menu_ProcessInput`, `Menu_InitCursor`, `Menu_MoveCursor`, `DrawStdFrameWithCustomTileAndPalette`, `CreateYesNoMenu`) was previously stubbed out in `battle_engine_stubs.c` returning 0, causing dialogue selection menus (such as Professor Oak's rival name choice box) to immediately choose option 0 (`NEW NAME`) and skip user interaction. `src/menu.c` is linked in `Makefile.native` with all duplicate stubs removed.
21. **Script command-table end must be a derived pointer, never a separate array.** The interpreter bounds-checks every opcode with `cmdFunc >= ctx->cmdTableEnd`. `gScriptCmdTableEnd` was a separate 1-element `ScrCmdFunc[]` array; the macOS linker placed it ~500 KB BEFORE `gScriptCmdTable`, so every command failed the bounds check, `ScriptContext_RunScript()` shut down instantly, and all field interactions (A-press on NPCs/signposts) silently did nothing — movement still worked because it bypasses the interpreter. It is now `const ScrCmdFunc *const gScriptCmdTableEnd = gScriptCmdTable + ARRAY_COUNT(gScriptCmdTable)` in `src/data/script_cmd_table.h`. `ScriptContext_SetupScript`/`RunScriptImmediately` must pass `gScriptCmdTableEnd` as a VALUE (no `&`).
22. **`Makefile.native` has no header dependency tracking (no `.d` files).** Changing a header included by an ENGINE_SRCS file does NOT recompile that object — `make` silently links stale code. This bit the script-table fix: `src/data/script_cmd_table.h` changed but `scrcmd.o` didn't rebuild. After editing any header included by a built source, `touch` the including `.c` file or `make -B`. A dependency-file rule is future work.
23. **The linker requires pointer relocations to be aligned.** Emitting `const void *` fields inside `__attribute__((packed))` structs (or byte-spilled `(u8)((uintptr_t)&sym >> n)` arrays) fails with `ld: pointer not aligned`. Native scripts therefore store pointer references as u32 INDICES into the aligned `gNativeScriptPtrs[]` table generated by `tools/gen_map_data.py`; `ScriptReadPtr()` in `src/script.c` resolves `gNativeScriptPtrs[ScriptReadWord(ctx)]`. Opcodes carrying pointers must call `ScriptReadPtr`, never `ScriptReadWord`.
24. **Pointer-typed operand ABI for native scripts.** `ScriptContext.data` is `uintptr_t[4]` on the host. Commands whose operands are addresses (`goto`/`call`/`goto_if*`/`call_if*`/`loadword` with labels, `applymovement`, `msgbox`, `special` table lookups) read host-width pointers via `ScriptReadPtr`; numeric/virtual operands (`vgoto`, `vmessage`, plain `.2byte`) stay `ScriptReadHalfword`/`ScriptReadWord`.
25. **macOS/SDL window-repaint quirk.** Occasionally a freshly launched window shows a stale mid-fade frame (a gray FireRed logo) even though the engine loop is healthy — confirmed via `sample`: PPU rendering, texture upload, and frame pacing all tick while the window shows the stale frame. Clicking/focusing the window redraws it. Not an engine bug; do not chase it in PPU/renderer code.
26. **Host pointers cannot be split across 16-bit task-data slots.** `SetWordTaskArg()` stores two 16-bit slots, so `SetWordTaskArg(taskId, N, (uintptr_t)ptr)` silently truncates on 64-bit. `Task_AnimateDoor` (`src/field_door.c`) packed `struct DoorGraphics *` and `struct DoorAnimFrame *` into two slots each and rebuilt a 32-bit pointer, so the FIRST door animation (stepping out of the front door) dereferenced garbage and crashed. Both operands are now stored whole with `memcpy` (`tFramesPtr`/`tGfxPtr`, four slots each on the host). The same trap existed in `src/new_menu_helpers.c` (`DecompressAndLoadBgGfxUsingHeap*` → `TaskFreeBufAfterCopyingTileDataToVram`), which freed a truncated buffer during the map transition; that path now uses the new `SetPointerTaskArg()`/`GetPointerTaskArg()` (`include/task.h`, `src/task.c`). Other `SetWordTaskArg(..., (uintptr_t)…)` sites remain in files that are not linked yet (`easy_chat_2.c`, `intro.c`, `pokemon_jump.c`, `pokemon_special_anim*.c`, `shop.c`) and in `src/fldeff_flash.c` (map preview, excluded under `PORTABLE`). Fix them the same way before linking those features.
27. **A coord event must never fall back to `sDummyScript`.** An inline `end` script is NOT inert for an automatic proximity trigger: `ProcessPlayerFieldInput()` → `TryStartStepBasedScript()` re-runs it every frame the player stands on the tile, `DoCB1_Overworld()` re-locks the field each time, and the player can never start another step — a permanent freeze. This is what stopped the player dead on Pallet Town's `(12,1)`/`(13,1)` Oak trigger. `tools/gen_map_data.py` now emits `.script = NULL` for any coord event whose script is not compiled natively; `TryRunCoordEventScript()` handles NULL (runs the weather trigger and returns) so the tile is simply walkable. Object and BG events keep `sDummyScript` — an A-press that does nothing is harmless there.
28. **Unported field effects must be no-ops, and the stub table must be full-size.** *(Superseded in part — field effects are now ported; see fix #49 for the script table and `FieldEffectScript_ReadWord`.)* `FieldEffectStart()` indexes `gFieldEffectScriptPointers[fldeff]` and then calls `sFldEffScrcmdTable[*script]`. The stub table was a single `{ NULL }` entry, so the tall-grass ground effect on the first step into Route 1 read out of bounds and jumped through a garbage command pointer. `FieldEffectStart()` returns 0 for a NULL script WITHOUT adding the id to the active list (otherwise waiters would hang forever) — that guard is still what keeps every not-yet-ported effect inert.
29. **Wild encounters must not start while the battle engine is stubbed out.** *(Superseded - the battle engine is now linked; `StandardWildEncounter()`'s `PORTABLE` early-out and the `StartWildBattle()` stub were both removed when encounters were re-enabled.)* `CheckStandardWildEncounter()` → `StandardWildEncounter()` → `StartWildBattle()` (a no-op stub) still returned TRUE, so `DoCB1_Overworld()` locked the field expecting a battle to unlock it later — walking into Route 1's tall grass froze the game with `locked=1, scriptRunning=0`. `StandardWildEncounter()` returns FALSE under `PORTABLE` (the battle engine is not linked); `StartWildBattle()`/`StartRoamerBattle()` remain stubs.
30. **Outdoor maps need generated `MapConnections`.** `MapHeader.connections` was hardcoded NULL, so `gMapConnectionFlags` stayed all-false, `GetMapBorderIdAt()` answered `CONNECTION_INVALID` in every direction (walling the player inside Pallet Town) and `InitBackupMapLayoutConnections()` had nothing to fill the neighbouring map into `VMap`. `tools/gen_map_data.py` now emits `struct MapConnection`/`struct MapConnections` from each `map.json` `connections` array (`up/down/left/right` → `CONNECTION_NORTH/SOUTH/WEST/EAST`, with `offset`, `MAP_GROUP()`/`MAP_NUM()`), which both draws the neighbour across the border and lets `CameraMove()` hand off via `LoadMapFromCameraTransition()`.
31. **Sprites whose top is above the screen must be clipped, not dropped.** `DrawSprites()` (`src/platform/ppu.c`) converted a wrapped OAM y (`y >= DISPLAY_HEIGHT` → `y -= 256`, i.e. 248 → −8) and then compared it against the scanline as `vcount >= (unsigned int)(y - half_height)`, so any sprite with a negative top failed the test and was skipped entirely instead of having its visible rows drawn. Overworld sprites use `coordOffsetEnabled` (map-space `sprite->y` plus `gSpriteCoordOffsetY` at OAM build time), so walking south in `PalletTown_PlayersHouse_1F` gives Mom at `(8,4)` an OAM y of 248 the moment the player reaches the room's south-most walkable row `(x,8)` — `CameraUpdate()` keeps the player ~4.5 tiles down the screen and pans 16 px per tile, and the room is only 10 tiles tall. She vanished for one row and returned one step north; the engine was not hiding her (`invisible=0`, `offScreen=0`, `oam.y=248`). The window check is now signed (`(int32_t)vcount >= y - half_height && (int32_t)vcount < y + half_height`); sprites fully above the screen (OAM y ≥ 256 − height) still stay hidden. Verified with a scripted `--skip-intro` walk (bedroom → 2F stairwell `(10,2)`, hold west → 1F → down to `(10,8)`): the pre-fix capture has no Mom, the post-fix capture draws her clipped at the top edge.
32. **The native `gSpecials` table must stay index-aligned with `data/specials.inc`.** `tools/gen_map_data.py` encodes a `special` as the opcode plus the special's *position* in the real specials table (`data/specials.inc`), and `ScrCmd_special`/`ScrCmd_specialvar` index `gSpecials` with it. The stub table in `src/platform/overworld_stubs.c` used to hold one entry (`HealPlayerParty`, index 0 by luck), so any other special was out of range and `AGB_ASSERT_EX` → `abort()` (fix the player sees: reading Oak's lab sign killed the game). Entries are now designated initializers keyed by the real index (`[0]`, `[368]`, `[369]`, `[371]`), unported slots stay `NULL`, and the `PORTABLE` dispatch prints `[Script] special N is not ported` instead of calling through `NULL` or aborting. Adding a `special` to a compiled scene means adding its entry here; if `data/specials.inc` ever changes, the generator re-emits a different index and the table reports the miss loudly rather than jumping into a neighbouring function.
33. **FRLG reads signs by walking into them, and walking away must close the box.** FireRed/LeafGreen (unlike Ruby/Sapphire/Emerald) open a signpost's text when the player holds the direction he is facing into the sign — `ProcessPlayerFieldInput()` checks held input (`heldDirection`/`checkStandardWildEncounter` → `TrySetUpWalkIntoSignpostScript`) *before* it checks `pressedAButton`. That path was already correct here. What was missing was the dismissal: `FieldInput_HandleCancelSignpost()` pushes a direction *away* from the sign, calls `ScriptContext_SetupScript(EventScript_CancelMessageBox)`, and locks the field, but `EventScript_CancelMessageBox` was one of the dummy `{ 0x02 }` scripts in `src/platform/overworld_stubs.c`, so the script did nothing and the signpost frame stayed drawn over the world for ~65 frames of walking. The real 3-byte script (`special DoPicboxCancel`, `release`, `end`) is now compiled from `data/event_scripts.s` (added to `NATIVE_SCRIPT_ROOTS`) and special 346 (`DoPicboxCancel`) is a native entry in `gSpecials` — it mirrors `src/field_specials.c` minus its `PicboxCancel()` call, since `src/script_menu.c` (`ScriptMenu_ShowPokemonPic`) is not linked and that window can never be open. Verified under `--skip-intro` with a temporary warp + input probe: with the stub, a held-into sign at frame 245 shows `mb_hidden=0 mb_type=2 signpost=1` and the box survives until frame 320 as the player walks away; with the real script the box is hidden the same frame the cancel script runs (`mb_hidden=1 mb_type=0`) and the PPU capture shows no box. Both the held-direction walk-into and the A-press paths open the box in the real signpost frame (`LoadSignpostWindowFrameGfx`). `TestOverworldInteractions()` (`src/platform/main.c`, runs on every launch) now also asserts the cancel script's opcode sequence, so a reintroduced dummy stub fails loudly instead of silently leaving the box on screen.
34. **Object-event sprite sheets need `spritesheet_rules.mk`, not plain gbagfx conversion.** `src/data/object_events/object_event_graphics.h` bakes every overworld sprite in with `INCBIN`, and `overworld_frame(ptr, 2, 4, frame)` addresses frame *i* at byte offset `i*256` assuming GBA column-major tiles *inside* each frame. `gbagfx` only emits that layout when given pret's per-sprite `-mwidth/-mheight` flags; a plain conversion emits row-major tiles and the frames land in the wrong places. `Makefile.native` had explicit flag rules for only three sprites (`red_normal`, `green_normal`, `mom`), so `woman_1`, `fat_man` and `prof_oak` — and 126 others — were row-major and their first frames decoded to all-transparent tiles. Symptom: Pallet Town's sign lady and fat man were invisible while the player and Mom rendered fine; a runtime probe showed `tilesum=0` in OAM VRAM for the NPCs (vs 16609 for the player), and `srcsum=0` on the copy request proved the *source* frame was blank rather than the DMA path. Auditing all 157 object-event sprites against their canonical rules found 129 mismatches. `Makefile.native` now does `include spritesheet_rules.mk` (plus a plain `%.4bpp: %.png` fallback for the ~25 sprites pret gives no rule) and `assets` depends on `$(OBJ_EVENT_PICS)`, so tile order comes from pret's own table and cannot drift. `spritesheet_rules.mk` defines concrete targets, so the Makefile pins `.DEFAULT_GOAL := all`. After regeneration all 157 sprites byte-match canonical conversion. Four assets (`birth_island_stone`, `seagallop`, `ss_anne`, `old_man_lying_down`) still have a blank frame 0 — that is correct; their source PNGs are transparent there.
35. **Map script macros in movement definitions (`.macro ... .endm`).** In pret sources such as `data/maps/PalletTown/scripts.inc`, multi-step movement routines (such as `walk_to_lab`) are written as assembler `.macro` blocks. `tools/gen_map_data.py`'s `parse_labels()` previously treated `.macro` directives as commands belonging to whatever label preceded them and failed on the unexpanded macro invocation. `parse_labels()` now collects macro definitions within the file scope and expands them when referenced in movement labels, ensuring all movement sequences compile cleanly into standard `MOVEMENT_ACTION_*` byte sequences.
36. **Map script headers carry pointer operands, not just tags.** `MapHeaderGetScriptTable`/`MapHeaderCheckScriptTable` (`src/script.c`) read a `.4byte` script pointer after each `map_script`/`map_script_2` entry; on the host that is a u32 index into `gNativeScriptPtrs` (fix #23), so both readers must call the index table rather than `T2_READ_PTR`. Without this, `ON_TRANSITION`/`ON_WARP_INTO_MAP_TABLE`/`ON_FRAME` tables resolve to a truncated address and the map silently runs garbage. `tools/gen_map_data.py`'s `MapScriptRegistry` emits the tables (with a `0x00` terminator, not `0x02`) and only assigns a map's `.mapScripts` when its table compiled cleanly; otherwise the map keeps `sEmptyMapScripts`.
  37. **A script command that stops the script context must leave something to restart it.** `ScrCmd_pokemart` (and its two decoration variants) call `CreatePokemartMenu()` and then `ScriptContext_Stop()`, expecting the shop UI to re-enable the context when the player leaves the menu. The shop UI (`src/shop.c` plus `src/item_menu.c` and its INCBIN assets) is not linked, so `CreatePokemartMenu` is a no-op stub and nothing ever calls `ScriptContext_Enable()` — pressing A on a Mart clerk locked the field permanently. Under `PORTABLE` the three commands now report `[Script] <name> is not ported` and leave the context running, so the clerk's dialogue completes and the player keeps control. Verified with a probe: `[Script] pokemart is not ported`, then `locked=0` and the player walked out of the Mart.

38. **Battle/anim/AI script operands need an index table, and `T1_READ_PTR`/`T2_READ_PTR` must resolve it.** The battle script sources (`.4byte` label operands) cannot be assembled for the host. `tools/gen_battle_data.py` emits pret's exact byte layout but stores a u32 **index** into the aligned `gNativeBattlePtrs[]` table in every 4-byte slot that holds an address; `include/global.h` defines `T1_READ_PTR`/`T2_READ_PTR` as `gNativeBattlePtrs[T*_READ_32(ptr)]` under `PORTABLE`. Numeric 4-byte operands keep their literal and stay on `T*_READ_32`, so the two kinds are disjoint per slot. **Pointer-vs-number classification is derived from the C readers, not hand-written:** `derive_opcode_readers()` finds each `Cmd_*` body via the dispatch table (authoritative — `tryfaintmon_spikes` encodes 0x19 handled by `Cmd_tryfaintmon`, and `pursuitrelated` 0xEC by `Cmd_pursuitdoubles`), and tracks the script pointer as a *set* of cursors because animation/AI handlers advance-then-read (`sBattleAnimScriptPtr++`) while battle handlers read at a fixed `gBattlescriptCurrInstr + N`. Nested macros keep their own operand numbering (`sethword` expands to two `setbyte` commands). A slot with no reader and no declared override is a hard error. Two slots are declared overrides because the engine never dereferences them: `copyfoestats`'s unused jump pointer and anim `jumpifcontest`'s.
39. **`T1_READ_PTR`/`T2_READ_PTR` are used for *all* battle pointer operands, including indirect ones.** `Cmd_accuracycheck` and `Cmd_jumpifaffectedbyprotect` read their jump target through the shared `JumpIfMoveFailed` helper, and `Cmd_createsprite`/`Cmd_createvisualtask`/`Cmd_createsoundtask` cast a `T2_READ_32` straight to a pointer. Overriding only `T2_READ_PTR` (or classifying by macro name) misses those; the anim cast sites were changed to use `T2_READ_PTR` so one rule covers them.
40. **C pointer arithmetic on a struct scales by `sizeof(struct)`; pret's script aliases are BYTE offsets.** `include/constants/battle_script_commands.h` defines operands like `#define sMOVEEND_STATE gBattleScripting + 0x14`; the assembler added a raw byte offset, so emitting `&gBattleScripting + 0x14` in C advances 20 *structs*. `tools/gen_battle_data.py` emits `(void *)((u8 *)&BASE + N)` for every such alias, so `sMULTIHIT_STRING` (+8) and `sMOVEEND_STATE` (+20) address the intended bytes.
41. **A pointer stored through a `u32` lvalue truncates on the host.** Two live instances came out of linking the battle engine: `GetWindowAttribute(winId, WINDOW_TILE_DATA)` returned a `u8 *tileData` through a `u32` return type, so `battle_interface.c`'s healthbox text copy read a truncated address (fixed with `GetWindowTileDataPtr()`, and the four call sites migrated); and `battle_setup.c`'s `TrainerBattleLoadArgs` wrote text-script pointers with `SetU32` through `u8 **` variables, leaving the upper 4 bytes stale (now `*(uintptr_t *)ptr` under `PORTABLE`). `src/pokeball.c` still has the sibling 16-bit task-slot split (`tCryTaskMonPtr1 = (u32)(mon) >> 16`) — see fix #26 for the `SetPointerTaskArg`/`GetPointerTaskArg` treatment it needs.
42. **Clear a scene's interrupt callbacks before freeing the state they read.** `battle_transition.c`'s `IsBattleTransitionDone()` freed `sTransitionData` and then let `CB2_InitBattle` install its own VBlank callback, but on this port `VBlankIntr()` is called directly from the frame loop rather than from an interrupt, so `VBlankCB_Slice` could still run once with `sTransitionData == NULL` and fault in `SetGpuReg(... sTransitionData->win0V)`. `SetVBlankCallback(NULL)`/`SetHBlankCallback(NULL)` now run before the free.
43. **GNU Make 3.81 rejects static pattern rules with a generated target list, and `define` is recursively expanded.** `$(OBJ_DIR)/%.o` rules for `PREPROC_SRCS` are emitted with `$(eval $(call ...))`; inside that, `$$` (not `$$$$`) is the correct escape — one `$$` survives `$(call)` and `$(eval)` as a literal `$`, so the recipe still expands `$@`/`$<` at build time. A recursively expanded `define` also consumes `$(1)` before `$(call)` can substitute it, so parameterised rule text must not put `$(N)` in the variable body.

44. **`struct Sprite.data` must be widened under `PORTABLE` to hold a pointer.** `StoreSpriteCallbackInData6` (`src/battle_anim_mons.c`) stores a `SpriteCallback` across `data[6]` and `data[7]`. With the GBA's `s16 data[8]` those two slots are exactly 4 bytes, so a 64-bit host function pointer loses its top half and `AnimateSprites()` jumps into unmapped memory (`EXC_BAD_ACCESS` at a low address like `0x7511a4`). `include/sprite.h` declares `s32 data[8]` under `PORTABLE` and the store/load uses `memcpy` of the whole pointer. Note `data[]` is never halfword-aliased elsewhere, so widening it is safe.
45. **A sprite pointer stored in a 16-bit task slot truncates the same way.** `src/pokeball.c` packed `struct Pokemon *mon` into `tCryTaskMonPtr1`/`tCryTaskMonPtr2` (`data[3]`/`data[4]`). Use `SetPointerTaskArg`/`GetPointerTaskArg` (fix #26) instead; the helpers take a `data[]` index, not the macro name.
46. **A stale stub whose *type* is wrong is worse than a missing symbol.** `src/platform/battle_engine_stubs.c` declared `const u8 gCB2_AfterEvolution[] = {0xFF}` while `include/evolution_scene.h` declares `extern void (*gCB2_AfterEvolution)(void)`. `evolution_scene.c` is not linked, so the stub was the live definition and landed in `.rodata`; `battle_main.c`'s `gCB2_AfterEvolution = BattleMainCB2` then wrote to a read-only page and faulted with `EXC_BAD_ACCESS (code=2)`. The first time this is reachable is the first completed battle, so it hid behind the battle-engine gaps. When stubbing a symbol, match the **declared type** (`void (*)(void) = NULL`), not a byte-array placeholder; a `const u8[]` compiles and links silently but cannot be assigned.
47. **`tools/gen_battle_data.py` macro expansion must honour `:vararg`, `.if/.else`, and end-minus-start label math.** Three related defects desynchronised every animation script, which parked the anim interpreter on `Cmd_nop` (0x06) — an empty handler that never advances `sBattleAnimScriptPtr`, so the `do/while` in `RunAnimScriptCommand` spun forever (symptom: any move animation hung the game). (a) A `:vararg` parameter must absorb *all* remaining arguments as one comma-separated list; `createsprite template, ANIM_ATTACKER, 2, 0, 0, ANIM_TARGET, 2` is 3 fixed params plus 4 varargs, and binding only the first dropped the rest. (b) `.if/.elseif/.else/.endif` inside a macro body must emit only the taken branch — emitting both duplicated `createsprite`'s `ANIMSPRITE_IS_TARGET` byte. (c) `layout_byte_expression` computes the vararg count from `(.Lend - .Lstart)`; the operands are (end, start), so the subtraction is `labels[first] - labels[second]`. All three are anim-only: `battle_data.h` and `ai_data.h` were byte-identical before and after. File-level `.if` had a fourth bug (`.else` never cleared the skip flag, so the else-branch was dropped entirely — `Move_EXPLOSION` lost its `createsprite`); file-level and macro-body conditionals now share one `filter_conditionals()` helper.
48. **Script pointer operands read by `TrainerBattleLoadArgs` need the index table too.** `sTrainerAIntroSpeech`/`sTrainerADefeatSpeech`/`sTrainerVictorySpeech` are loaded with `TRAINER_PARAM_LOAD_VAL_32BIT`, i.e. `T1_READ_32` — but the generated trainer scripts store a u32 **index** into `gNativeScriptPtrs`, not a pointer. `SetU32` now resolves `gNativeScriptPtrs[value]`, and `TRAINER_PARAM_CLEAR_VAL_32BIT` writes `NULL` via `SetPtr` instead (index 0 is a real table entry, so clearing through `SetU32` would install an unrelated script).
49. **Field-effect scripts need their own index table, and `FieldEffectScript_ReadWord` must return host-width.** `data/field_effect_scripts.s` cannot be assembled for the host, so `tools/gen_field_effect_data.py` compiles it to native C byte-for-byte (pret's opcodes and operand widths). Two traps, both 64-bit:
    - The 4-byte address operands (`loadfadedpal`/`loadpal` → `struct SpritePalette *`, `callnative` → function pointer) are emitted as u32 **indices** into the aligned `gNativeFieldEffectPtrs[]`, like `gNativeScriptPtrs`/`gNativeBattlePtrs`. Index 0 is reserved as NULL so a misclassified operand cannot alias a real symbol.
    - `FieldEffectScript_ReadWord()` was `static u32`, and every caller casts its result straight to a struct pointer or a function pointer, so the top half of a host pointer was discarded. It now returns `uintptr_t`.
    
    Which `callnative` targets are emitted is decided by **scanning the linked sources**: the generator reads `ENGINE_SRCS`/`PREPROC_SRCS` from `Makefile.native`, walks the `#include` closure (palettes and templates live in headers such as `src/data/field_effects/field_effect_objects.h`, not in the `.c`), and requires a real *definition* — a header's `extern u32 FldEff_X(void);` is not one. Effects whose native is not linked stay NULL and simply do not start, exactly as the old all-NULL stub behaved. A hand-maintained "ported natives" list would go stale and turn a missing symbol into a link error or a crash.
    
    Two follow-ons when linking a `.c` that defines field effects: delete the stub definitions it now provides (`src/platform/overworld_stubs.c` had 26, including `UpdateTallGrassFieldEffect`/`WaitFieldEffectSpriteAnim`/`CreateWarpArrowSprite`), and re-run the generator so the newly-resolvable effects enter the table. The generator emits the `extern` declarations its table needs, so no header edit is required.

50. **`SetTaskFuncWithFollowupFunc` splits a pointer across two 16-bit slots.** pret stores the follow-up `TaskFunc` in `data[NUM_TASK_DATA - 2]` / `[+1]`, which is exactly 4 bytes and therefore truncates a 64-bit host function pointer on `>> 16` — the next `RunTasks()` calls `SwitchTaskToFollowupFunc` and jumps into unmapped memory with `EXC_BAD_ACCESS` at a low address. Nothing caught it before because no linked code called it. It **cannot** reuse `data[12..15]`: `src/item_menu.c`'s pocket-switch task keeps `tSwitchCounter`/`tSwitchState` in `data[12]`/`data[13]`, so parking the pointer there would be clobbered mid-switch. The pointer now lives in a file-static `sTaskFollowupFuncs[NUM_TASKS]` side table keyed by `taskId` (declared and read only in `src/task.c`, cleared in `DestroyTask`), so no task's `data` layout shifts. This is what blocked the START menu, BAG, and party menu — `src/start_menu.c`, `src/item_menu.c` and `src/party_menu.c` all use the follow-up pattern. Fix future instances the same way; never widen `struct Task.data` for this, since the slots are also genuinely used as counters.

51. **A software PPU that ignores OAM affine matrices, display windows and HBlank DMA silently drops whole animations.** Three GBA subsystems the port originally stubbed out, each with a distinct symptom:
    - **Affine OAM sprites.** `gOamMatrices` was never read, so every rotated/scaled sprite drew upright and unscaled. The user-visible bug: the Poké Ball flew without spinning (the matrices were computed by `SpriteCB_BallThrow` but had no effect). The compositor now inverse-transforms each pixel through `gOamMatrices[oam.matrixNum & 0x1F]`. Double-size affine sprites (`(oam->affineMode >> 1) & 1`) cover a doubled bounding box but index their tile block using the object's **undoubled** dimensions, so `block_offset`'s stride uses `src_width`, not the doubled `width` — using the doubled width samples the wrong tiles. Flip is meaningless for affine sprites (the matrix carries the sign) and must not be applied on top.
    - **Display windows (`REG_WIN0H`/`REG_WIN0V`/`REG_WININ`/`REG_WINOUT`).** Zero references before; every window-masked screen composited as if unmasked. The battle transitions wipe by animating WIN0, and the OPTION menu dims everything except the cursor row, so both rendered as a plain fade. `ComputeWindowMask()` resolves region priority WIN0 > WIN1 > OBJWIN > outside per pixel. Two traps: masking must be **inactive** unless a window is actually switched on in `REG_DISPCNT` (else a stale `REG_WINOUT` mutes the whole screen), and an OBJ-window sprite is **not drawn** — its opaque pixels only mark the region (regardless of its blend/priority bits), so the old code's "draw it if WINOUT allows" branch was wrong.
    - **HBlank-timed DMA.** `DmaSet` compiled to `do {} while (0)` under `PORTABLE`, so per-scanline effects never wrote their register. `PortableDmaSet` now records the channel and `Platform_RunHBlankDma()` replays one transfer per rendered scanline from inside `PPU_RenderScanline`. Only `DMA_START_HBLANK` channels are recorded; `DMA_START_NOW` still runs inline via `PortableDmaCopy`. The replay must run **after** the scanline is composed and takes effect on the *next* one — the engine's VBlank callback arms the channel from `buffer + 1` precisely because the first entry is consumed by the hand-written register store, which is why `RenderFrame` seeds the buffer one entry in. The channel's source advances and its destination stays put (DMA0's destination is fixed/reload, not incrementing — see fix #52).

    All three are pure compositor gaps, not engine gaps: the engine computed the matrices and wrote the registers correctly the whole time. When a whole class of animation is missing but the engine logic runs, check whether the PPU reads the register the engine writes.

52. **The DMA control word packs flags in the high halfword and the transfer count in the low one.** `Platform_RunHBlankDma()` read its source-step and destination-step bits straight out of `ch->control` (`(control >> 7) & 3`, `(control >> 5) & 3`), which reads the **count** — 1 for every HBlank arm site — not the flags. That decoded `B_TRANS_DMA_FLAGS` (`count 1`, `DMA_SRC_INC`, `DMA_DEST_FIXED`) as "source increments, destination increments", so each scanline's transfer wrote one register further along: scanline 0 hit `REG_WIN0H`, then `WIN0V`, `WIN1H`, `WIN1V`, `BLDCNT`, `BLDALPHA`, `BLDY`, `MOSAIC`, and on into the rest of `REG_BASE`. Register dumps showed every one of those registers holding the walking source values (`47F1`, `00A9`, `61F1`, …). The window then had no horizontal bounds and the blend registers were garbage, so the field was masked to near-black with only a few slices of background surviving — the "artifacting" seen when entering a wild battle (Slice and White-Bars-Fade both use WIN0H via DMA). It also silently corrupted the window/blend state for the whole transition, and `BLDALPHA`'s clobber would have degraded any simultaneous blend.

    The flags must come from the high halfword: `ctrl = ch->control >> 16`, then `(ctrl >> 7) & 3` / `(ctrl >> 5) & 3`. Every HBlank arm site in the engine is DMA0 with `DMA_DEST_FIXED` (`B_TRANS_DMA_FLAGS`) or `DMA_DEST_RELOAD` (`SCANLINE_EFFECT_DMACNT_16BIT`/`_32BIT`), and the count is always 1: the source walks the per-scanline buffer while the write stays pinned to one register, so `count` only ever means "one register write per scanline". The destination step is honoured as fixed/reload (2 and 3) and increment (0); DMA0 is the only channel the engine arms this way.

    `TestOverworldInteractions()` now arms a fake HBlank channel at a 4-entry destination array and asserts the transfer lands on entry 0 every scanline with entries 1–3 untouched, so a reintroduced low-halfword read fails loudly at startup rather than at the next battle transition.

53. **A green build on one machine is not a buildable tree.** `make -f Makefile.native` worked here only because this working copy carried artifacts a fresh clone never has — and every one of them was gitignored, so nothing produced them. A `git clone` + `make` failed at the first asset rule and kept failing deeper in. Four separate gaps, each capable of hiding the others:

    - **The C tools were never built.** `tools/gbagfx` and `tools/preproc` are build products (each has its own `.gitignore`), and `Makefile.native` used them without ever building them. Same for `tools/mapjson` and `tools/jsonproc`, needed for the generated headers. They are now built on demand, and a `tools` target runs a sub-make for them before the asset phase — pret's rule files use pattern rules (and static pattern rules whose target list is a generated variable), so there is no single point where a `| $(GFX)` prerequisite can cover every asset recipe, and adding it to the tool's own rule is a self-dependency that make drops with a warning while still scheduling the asset first.
    - **The generated constants headers were never generated.** `include/constants/{map_groups,layouts,map_event_ids,region_map_sections,heal_locations}.h` and the `src/data/*` equivalents come from `data/maps/map_groups.json`, `data/layouts/layouts.json`, every `map.json` and the `region_map_sections`/`heal_locations`/`items`/`wild_encounters` JSON, via `mapjson`/`jsonproc`. All are gitignored, so a clone had none and anything naming a map (`include/constants/maps.h` → `map_groups.h`) failed to compile. Regenerating them with the same tools and inputs reproduces the checked-in pret output byte-for-byte.
    - **Most of pret's implicit conversions were missing.** Only `%.4bpp: %.png` existed; `.8bpp`/`.1bpp` from PNG, `.gbapal` from PAL and PNG, and the `%.lz: %` chain were absent, and pret's `graphics_file_rules.mk`/`tileset_rules.mk` — which carry the per-asset flags — were not included at all. That is not just a missing-rule problem: without those files, nine battle-terrain sheets converted without `-num_tiles` (2560 bytes instead of 2464) and the five split anim sheets were combined into one PNG before conversion instead of concatenating per-part tile data, producing sheets far larger than `gBattleAnimPicTable` declares (`flower` 768 bytes vs the declared `0x00a0` = 160). Both classes now convert to the declared sizes. **Rule flags are correctness, not cosmetics** — an asset that converts "successfully" can still be the wrong length for the engine that reads it.
    - **Object compilation raced the assets.** Every object `INCBIN`s generated graphics during preprocessing, but `all` built `assets` and the objects in parallel, so a clean tree opened files the asset rules had not created yet (the `Failed to open "…"` in the preprocessor). `$(OBJS)` now depends on `assets` order-only.

    Two structural points worth keeping. The asset list is **derived, not hand-written**: `tools/list_incbin_assets.py` scans `src/` for `INCBIN` targets, drops the ones git tracks, and resolves the rest against the build's own rules, so it covers both single-source conversions and assets assembled from other assets (`graphics/unused/obi1.4bpp` is a `cat` of converted PNGs, and `INCBIN_U8("a.4bpp", "b.4bpp")` names two files). It must not invoke make — it runs from inside the Makefile. And `graphics_file_rules.mk` is pret's file kept verbatim except for one deletion: its `redyellowgreen_frame.bin` rule `cat`s four frame `.bin` files that exist neither here nor upstream while the concatenated result **is** tracked. That dead rule cannot be neutralised from `Makefile.native`, because prerequisites accumulate across rules and an empty recipe on the inputs makes `cat` run and truncate the tracked file — which is exactly what a first attempt did.

    Verify build-system changes from a real clone (`git clone . /tmp/x && cd /tmp/x && make -f Makefile.native`), never from the working tree: the untracked artifacts here make local success meaningless.

54. **A big prerequisite list on a target the top-level make walks makes GNU Make 3.81 spin under `-j`.** The symptom is unmistakable once seen: `make -f Makefile.native -j8` never finishes, `sample` shows make recursing inside its own dependency code (`+0x15e0c` ↔ `+0x17714`), **no child process is ever started**, and CPU sits at 100% (or, on some runs, wall time is far larger than user time because make is stat-ing). The same tree builds in ~29 s with `-j1`. It is not a cycle — make prints no circular-dependency warning, and `overriding commands` is 0.

    The trigger is list *size*, and bisection pinned it exactly: a target carrying 1178 prerequisites is fine; 1179 spins. The 1179th entry was an ordinary `graphics/battle_anims/backgrounds/attract.4bpp.lz`, so it is a threshold, not a special file. Concretely, `assets: … $(INCBIN_ASSETS) …` with the derived list (~6400 paths) was enough on its own; removing just that token from the line turned a >60 s wedge into 0.6 s with no other change.

    Structure that avoids it (and the reason `Makefile.native` looks the way it does):
    - The large list is never a prerequisite in the graph object compilation walks. `$(OBJS)` takes one real-file stamp (`build/native/.assets.stamp`) order-only; the stamp's recipe drives the two asset phases through sub-makes.
    - Those sub-makes **must** pass `-j1`. A sub-make inherits `MAKEFLAGS`, so a parallel one reproduces the identical spin internally — the first attempt at this fix hung for exactly that reason.
    - `all` does not name the phony `assets` target: a phony target with thousands of prerequisites gets re-walked from every dependent.

    Related trap found in the same file: `.PHONY` on a *generated* target makes it unconditionally out of date. The five split anim sheets were declared phony, so every build re-ran `cat` over the sheet parts, touched the sheet, and forced its `.4bpp.lz` and everything downstream — turning a 10 s incremental build into the wedge above. `graphics_file_rules.mk` already declares real prerequisites for those `cat` rules, so the phony declaration was pure harm.

    When a parallel build spins with no children, reach for this before suspecting the compiler: check for a target with an enormous prerequisite list, and test the same tree with `-j1` to confirm it is make's graph walk rather than the build itself.

### Native Frame Timing
- **The game clock is not the display clock.** The original native loop relied on `SDL_RENDERER_PRESENTVSYNC` without an independent timer; that allowed high-refresh presentation to accelerate frame-based gameplay, while the fallback renderer did not explicitly request VSync. This timing gap was found after movement appeared too fast. The pre-fix runtime frame rate was not measured.
- **Target the hardware frame period:** 280,896 CPU cycles per frame at 16,777,216 Hz, or approximately 59.7275 Hz (16.7427 ms per tick). Do not change movement speeds, animation counters, or emulate CPU clock speed to compensate for host performance.
- **One pacing owner:** `WaitForFrameDeadline()` uses `SDL_GetPerformanceCounter()` / `SDL_GetPerformanceFrequency()` and absolute deadlines. Time spent in the engine and renderer counts toward the next deadline; `SDL_Delay()` sleeps rather than busy-spins. Do not add a fixed sleep after every frame or a second limiter in `WaitForVBlank()`, the BIOS shim, or the standalone test.
- **VSync is deliberately disabled** in renderer creation so monitor refresh does not set engine speed. Do not re-enable it as a replacement for the game clock; any future presentation changes must preserve independent simulation timing.
- **Stalls do not accumulate catch-up ticks.** When the next deadline is already past, the clock resynchronizes to one frame after the current time. Sustained overload slows the game rather than skipping engine updates.
- **Verification:** Metal and software renderers were exercised with 4 ms of simulated engine work per frame. For 300 frames, elapsed times were 5.022 s and 5.049 s respectively (target 5.023 s). Both recovered from a 500 ms stall without a catch-up burst. A real 9,300-frame engine boot completed in 155.92 s (frame-time target 155.71 s plus startup), with bedroom spawn confirmed in the capture. The standalone 120-frame PPU test also passed. Temporary timing probes were removed.

### Debugging Tips
- **Boot tests now run at game speed:** allow roughly `N / 59.7275` seconds plus startup for `--boot-test N`; 9,300 frames take about 156 seconds. Use a timeout above that duration. `--test` renders 120 paced frames in about two seconds; its old extra `SDL_Delay(16)` was removed.
- Boot test with N frames: `./firered-native --boot-test N` (saves `engine_boot_output.bmp` at frame N). The boot test auto-presses: START at frames 5–10 (skip intro fade), 30–35 (enter game from Title Screen), DOWN at 230–231 + A at 240–245 (select NEW GAME), then A at 320–325 / 360–365 / 400–405 / 520–525 / 560–565 (advance Controls Guide → Pikachu intro → Oak speech). Starting at frame 620 it also presses A for 6 frames every 90 frames. This periodic input advances Oak's dialogue, gender selection, player naming, rival naming (frames 6500–7200), farewell speech (frame 8600), hands off to `CB2_NewGame` by frame ~8900–9000, and spawns into the player's bedroom in Pallet Town (`CB2_Overworld`) by frame 9200.
- Fast overworld iteration: `./firered-native --skip-intro --boot-test 200` reaches the bedroom (fresh save, `RED`/`GREEN`) in ~3.5 s instead of ~156 s for the 9,300-frame full boot. Byte-identical captures across runs (RNG is seeded from a host-zeroed timer).
- Past-the-rival iteration: `./firered-native --post-rival --boot-test 200` reaches Oak's Lab with the starter and the first rival battle already done in ~3.5 s as well. It shares the `--skip-intro` save setup, so the two flags are ordered/interchangeable, but `--post-rival` suppresses the bedroom NES replay (UP at 60–65, A at 90–95) that `--boot-test` runs under `--skip-intro` — that interaction does not exist in the lab, and replaying it there would just be dropped input.
- Missing-symbol workflow: `make -f Makefile.native` then extract `grep -oE '"_[A-Za-z0-9_]+"'` from linker output; find real definitions with `grep -rln "SymbolName" src/*.c`; add that file to ENGINE_SRCS (or PREPROC_SRCS if it has `INCBIN`/`_()`) and DELETE the stub version from `stubs.c`. Battle-engine symbols now come from the real sources; only two stub files remain, and they must stay disjoint: `src/platform/battle_engine_stubs.c` keeps the non-battle scene substitutes and tripwires, while `src/platform/battle_peripheral_stubs.c` holds the ~119 peripherals a wild battle never reaches (safari, Poké Dude tutorial, Teachy TV, Union Room, quest log, Easy Chat, field moves).
- **A green incremental build is not proof the tree compiles.** There is no header dependency tracking (fix #22) and object files do not depend on `CFLAGS` or on the source-list variables, so `make` can reuse objects that a changed Makefile should have rebuilt. Removing `string_util.c`'s preproc rule produced a clean-looking incremental build while the stale object was still in place; only `rm -rf build && make -f Makefile.native` exposed it. Verify build-system changes from scratch. A missing preproc source fails loudly (`_(x)`/`__(x)`/`INCBIN` are undefined outside the IDE/Cygwin guard in `global.h`), never silently.
- Field-effect data regeneration: `python3 tools/gen_field_effect_data.py` rewrites `src/data/field_effects/{scripts_data.h,ptr_table.c}`. Deterministic, tracked in git, and NOT regenerated automatically — re-run it after editing `data/field_effect_scripts.s`, after adding a field-effect `.c` to `Makefile.native`, or after deleting a stub that shadowed a native. It prints the effects it left NULL and why (the native is not linked).
- Battle data regeneration: `python3 tools/gen_battle_data.py` rewrites `src/data/battle/{battle_data,anim_data,ai_data}.h` and `ptr_table.c`. Output is deterministic (verified by hash), and the generated files ARE tracked in git, matching the `src/data/maps_data.h` convention. `Makefile.native` does not regenerate them automatically; re-run the tool after editing `data/battle_scripts_*.s` or the readers in `battle_script_commands.c`.
- Driving a scripted battle: message boxes and many controller states wait on `JOY_NEW(A_BUTTON|B_BUTTON)`, which needs a fresh press *edge*. A held key never satisfies it - inject a press/release cadence (e.g. 4 frames on, 4 off) through `REG_KEYINPUT` in `WaitForVBlank`.
- A `SIGSEGV`/`SIGBUS` backtrace handler is installed in `src/platform/main.c` (`CrashHandler`) — crashes print a symbolized stack trace.
- **Headless probes: write `REG_KEYINPUT` after `Platform_UpdateInput()`, before `ReadKeys()`.** The loop is `ReadKeys()` … `WaitForVBlank()`, and `Platform_UpdateInput()` (called from `WaitForVBlank`) overwrites the register with the real SDL state every frame. A probe writing it from inside `WaitForVBlank` is one frame late and immediately clobbered — the log shows the intended key while `gMain.heldKeys` stays 0 and the player never moves. Confirm with `gMain.heldKeys`, not `REG_KEYINPUT`.
- **The `--skip-intro` NES interaction leaves the field locked.** Frames 60–95 hold UP then A to replay the bedroom NES; that opens a dialogue script with `LockPlayerFieldControls()` and nothing dismisses it headless, so the rest of the run reports `locked=1`, `moving=0` and takes no steps. A movement probe on the dev save must warp before frame 60 or call `ScriptContext_Enable()` + `UnlockPlayerFieldControls()`. Warping or teleporting *onto* a wild-encounter tile also locks the field via `CheckStandardWildEncounter()`; start on a clear tile beside the grass and step in.
- **Verifying a field-effect probe:** `FieldEffectActiveListContains(FLDEFF_X)` is the precise check for "the effect started", and it is short-lived (the tall-grass rustle self-stops after its animation). Compare OBJ VRAM against the source asset to prove the graphics loaded, and diff a capture with the effect against one without to prove it renders — the tall-grass rustle showed up as an exact 16px-wide pixel span.
- `compile_flags.txt` at repo root configures clangd with `-DPORTABLE -DMODERN=1 -DFIRERED`; keep it in sync with `Makefile.native` CFLAGS.
---

## 4. Key Files & Structure

```text
openfirered/
├── Makefile.native            # Primary build script for native macOS ARM64 binary
├── include/
│   ├── gba/                   # GBA system headers (defines.h, io_reg.h, macro.h, etc.)
│   └── platform/
│       ├── platform.h         # Platform runner interface & memory declarations
│       └── ppu.h              # Software PPU interface
├── src/
│   ├── main.c                 # GBA main loop & AgbMain() entry point
│   ├── main_menu.c            # Main Menu scene (CONTINUE / NEW GAME)
│   ├── title_screen.c         # Title screen scene
│   ├── oak_speech.c           # Oak's Speech scene (NEW GAME flow)
│   ├── naming_screen.c        # Player and rival naming flows fully completed
│   ├── task.c                 # Task system; PORTABLE side table for follow-up funcs (fix #50)
│   ├── start_menu.c           # START menu (POKéDEX / POKéMON / BAG / PLAYER / SAVE / OPTION / EXIT)
│   ├── item_menu.c            # BAG menu: pockets, item list, context menus, registration
│   ├── option_menu.c          # OPTION menu (TEXT SPEED / BATTLE SCENE / SOUND / FRAME ...)
│   ├── party_menu.c           # POKéMON party screen
│   ├── pokemon.c              # Pokemon data, MonSpritesGfxManager, Spinda spots
│   ├── platform/
│   │   ├── system.c           # Memory buffer allocations (REG_BASE, VRAM_, PLTT_, gHeap, SaveBlocks)
│   │   ├── bios.c             # Portable C implementations of GBA BIOS SWI routines
│   │   ├── ppu.c              # Software scanline rasterizer (Mode 0 text BGs; Mode 1 text BGs + affine BG2; sprites and blending)
│   │   ├── sdl2.c             # SDL2 windowing, texture streaming, and input handling
│   │   ├── stubs.c            # M4A audio, link, save status, and remaining scene-transition stubs
│   │   ├── battle_engine_stubs.c  # Non-battle scene substitutes and loud-failure entry-point tripwires
│   │   ├── battle_peripheral_stubs.c  # ~119 battle-adjacent peripherals a wild battle never reaches (safari, tutorial, quest log)
│   │   ├── new_game_intro_text.c  # Charmap-encoded text tables from data/text/new_game_intro.inc
│   │   ├── dev_boot.c         # --skip-intro: fresh save straight into the bedroom
│   │   └── main.c             # Native entry point (main) with crash backtrace handler
│   ├── data/battle/           # GENERATED by tools/gen_battle_data.py (battle_data.h, anim_data.h, ai_data.h, ptr_table.c) - tracked, like maps_data.h
│   ├── data/field_effects/    # GENERATED by tools/gen_field_effect_data.py (scripts_data.h, ptr_table.c) - tracked
│   ├── field_effect.c         # Field-effect interpreter + FieldEffectStart/Stop
│   ├── field_effect_helpers.c # The per-effect natives (tall grass rustle, splash, dust, shadows...)
│   └── [engine subsystems]    # gpu_regs.c, palette.c, task.c, sprite.c, malloc.c, text.c, etc.
```

---

## 5. Build & Run Instructions

Prerequisites (installed via Homebrew):
```bash
brew install sdl2 libpng pkgconf
```

### Build native binary:
```bash
make -f Makefile.native
```
A fresh `git clone` builds with no extra setup. The build generates everything it consumes: the C tools (`tools/gbagfx`, `tools/preproc`, `tools/mapjson`, `tools/jsonproc`) are built on demand — a `tools` target runs a sub-make for them before any asset recipe, because pret's rule files use pattern rules that leave no place to attach them as a prerequisite — and the constants headers pret derives from JSON (`include/constants/map_groups.h`, `layouts.h`, `map_event_ids.h`, `region_map_sections.h`, `heal_locations.h` and the `src/data/*` ones) are regenerated from the same tools and inputs pret uses. `assets` is a prerequisite of every object (order-only), since each object `INCBIN`s generated graphics during preprocessing.

The asset list is derived, not hand-maintained: `tools/list_incbin_assets.py` scans `src/` for `INCBIN` targets, drops the ones git tracks, and resolves the rest against the build's own rules — including assets assembled from other assets, like the `graphics/unused/obi1.4bpp` `cat` chain. Targets it cannot resolve are reported on stderr as `[assets] no build rule for ...; skipping`. `python3 tools/list_incbin_assets.py --list-targets` prints the concrete targets the rule files name.

### Release build — no development FPS overlay:
```bash
rm -f build/native/platform/sdl2.o && make -f Makefile.native FPS_OVERLAY=0
```
`FPS_OVERLAY` defaults to `1` (`Makefile.native` line ~7; it appends `-DFPS_OVERLAY` to `CFLAGS`). `src/platform/sdl2.c` guards the FPS counters, glyph table, and both draw calls with `#ifdef FPS_OVERLAY`, so a `FPS_OVERLAY=0` binary contains no overlay code. **The stale `sdl2.o` must be deleted first** (or `touch src/platform/sdl2.c`): object files do not depend on `CFLAGS`, so `make` would otherwise keep the previously compiled overlay (same trap as fix #22). `-B` also works but rebuilds everything, assets included.

Verified: with `FPS_OVERLAY=0` the compile line carries no `-DFPS_OVERLAY`, `nm build/native/platform/sdl2.o` reports zero `fps` symbols, and the running window shows an overlay-free bedroom; the default build shows `FPS 60`.

### Run PPU standalone test (120 frames with test pattern & sprite):
```bash
./firered-native --test
```

### Run Engine boot test (60 frames through AgbMain):
```bash
./firered-native --boot-test
```

### Dev save — skip the intro and spawn in the bedroom:
```bash
./firered-native --skip-intro              # fresh save, bedroom, interactive
./firered-native --skip-intro --boot-test 200   # same, screenshot at frame 200
```
`--skip-intro` builds a fresh save (player `RED`, male, rival `GREEN`) and drops straight into `PalletTown_PlayersHouse_2F`, bypassing the title screen, main menu, controls guide, Oak's speech, and both naming screens. Use it for overworld iteration; use plain `--boot-test N` when the intro flow itself is under test. Flags may appear in any order.

### Dev save — past the starter and the rival battle, ready to leave Oak's Lab:
```bash
./firered-native --post-rival                  # fresh save, Oak's Lab, interactive
./firered-native --post-rival --boot-test 200  # same, screenshot at frame 200
```
`--post-rival` implies `--skip-intro` and goes one scene further: the player already has the Lv5 Bulbasaur (with the rival having taken Charmander), `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` is 4, `FLAG_BEAT_RIVAL_IN_OAKS_LAB` is set, and the game spawns in `MAP_PALLET_TOWN_PROFESSOR_OAKS_LAB` at `(6,8)` with controls unlocked — walk DOWN out the door and the world is open. Use it for overworld/battle iteration past the intro scenes; the state it writes is documented on `Platform_DevBootApplyStoryProgress()` in `src/platform/dev_boot.c`.

### Run Engine interactively:
```bash
./firered-native
```

---

## 6. Overworld Warp Status & Next Steps

The engine spawns into `PalletTown_PlayersHouse_2F`, unfreezes objects, unlocks player controls, and accepts D-pad movement cleanly.
1. **Downstairs Stair Warp (2F ↔ 1F): Complete**
   - `Makefile.native` links the real `src/fldeff_flash.c` map-transition callbacks and cave-transition assets. The empty `CB2_DoChangeMap` stub previously stranded the engine after fade-out instead of invoking `gMain.savedCallback` to load the destination.
   - A 10,800-frame native smoke run with temporary D-pad input verified arrival on 1F, completed fade, unlocked controls, walking away from the stairs, and returning to 2F. A PPU capture verified the downstairs room and Mom.
   - Party-menu Flash and map-preview screens remain excluded under `PORTABLE`; normal map loading and cave fade transitions use the real engine code. Cave transitions were not exercised by the stair smoke.
   - **Mom dialogue is real and interactive (completed):** `tools/gen_map_data.py` now compiles a script's full dependency closure from the ORIGINAL pret sources (`data/maps/*/scripts.inc`, `text.inc`, `data/scripts/**/*.inc`, `data/event_scripts.s`) into native bytecode inside `src/data/maps_data.h`. Unsupported commands fail loudly (stderr report) and fall back to `sDummyScript`. Mom's male/female/heal branches, movement, and `EventScript_OutOfCenterPartyHeal` all compile from source.
   - **Overworld object and TV interactions (completed):**
     - **1F TV (`PalletTown_PlayersHouse_1F_EventScript_TV`):** Interacting facing North plays gender-specific movie dialogue ("Four boys on railroad tracks..." / "Girl in pigtails on a brick road..."); interacting from sides displays "Oops, wrong side...".
     - **2F NES (`PalletTown_PlayersHouse_2F_EventScript_NES`):** Interacting facing North from (6, 6) towards (6, 5) plays "{PLAYER} played with the NES. ...Okay! It's time to go!".
     - **2F Signpost (`PalletTown_PlayersHouse_2F_EventScript_Sign`):** Posted notice sign at (11, 1) displays "It's a posted notice... If you're confused, ask for HELP! Press the L or R Button!".
     - **House Metatile Flavor Scripts:** Linked real implementations for `EventScript_Bookshelf` ("It's crammed full of POKéMON books"), `EventScript_Dresser` ("It's a nicely made dresser"), `EventScript_Cabinet` ("Dishes and plates are neatly lined up"), `EventScript_Kitchen` ("It smells delicious! Somebody's been cooking here"), and `EventScript_PlayerFacingTVScreen` ("There's a POKéMON on TV!"). Removed dummy stubs from `overworld_stubs.c` and updated `tools/gen_map_data.py` to collect `data/text/*.inc` sources and emit non-static symbols for global event scripts.
     - **2F PC:** The 2F PC script (`PalletTown_PlayersHouse_2F_EventScript_PC`) remains stubbed pending item storage / `player_pc.c` menu linkage.
   - **Interaction path for future scenes:** A-press → `FieldGetPlayerInput()` (gates: avatar `T_NOT_MOVING`/`T_TILE_CENTER`, speed != `PLAYER_SPEED_FASTEST`, no `PLAYER_AVATAR_FLAG_FORCED`) → `ProcessPlayerFieldInput()` → `GetInteractedObjectEventScript()`/`GetInteractedBackgroundEventScript()` → `ScriptContext_SetupScript()`. If an interaction silently does nothing but movement still works, suspect the script interpreter (see fix #21) or a stub script (`sDummyScript` = instant end).
   - **Object-event sprite sheets:** `Makefile.native` now builds every `graphics/object_events/pics/**/<name>.png` through pret's own `spritesheet_rules.mk` (see fix #34), so the `-mwidth/-mheight` tile order is correct automatically and `assets` regenerates them all. Do NOT hand-write gbagfx lines for new object-event graphics; add the sprite to `spritesheet_rules.mk` if pret has a rule for it. Palettes still need a `.pal → .gbapal` rule.
   - **Stub-vs-real scripts:** `src/platform/overworld_stubs.c` still defines dummy event scripts for every non-linked scene (`sDummyScript` = `{0x02}` instant-end). Interacting with TV/PC currently "succeeds" then instantly ends (no visible effect). When wiring a real scene, delete the stub definition and add the source (see also fix #8). `gStdScripts`/`gStdScriptsEnd` remain stubbed — Mom's default msgbox path does not need them.
   - **Specials:** `gSpecials` in `overworld_stubs.c` is native and `gSpecialsEnd` a one-past pointer; index 0 (`NativeSpecial_HealPlayerParty`) calls the real `HealPlayerParty()` from `src/script_pokemon_util.c`. Party-selection/egg-giving functions (`ScriptGiveMon`, `ScriptGiveEgg`, `CreateScriptedWildMon`, `ScriptSetMonMoveSlot`, `ReducePlayerPartyToThree`, `ChooseHalfPartyForBattle`) are `#ifndef PORTABLE`-guarded in `script_pokemon_util.c` with stubs retained in `overworld_stubs.c`.

2. **Pallet Town Exterior & Front-Door Warp (1F ↔ Outdoors): Complete**
   - Exit is the south arrow warp (`MB_SOUTH_ARROW_WARP` = `0x65`) on 1F's `(4,8)`/`(5,8)`; walking DOWN while standing on it (facing South) warps to `MAP_PALLET_TOWN` warp 0. Re-entering the house is the reverse: step onto Pallet Town's door tile `(6,7)` (`MB_WARP_DOOR`). BOTH directions play the real door animation (`Task_ExitDoor` → `FieldSetDoorOpened`/`FieldAnimateDoorOpen`/`FieldAnimateDoorClose`).
   - The `SIGSEGV` on stepping out the front door was the door-animation task storing 64-bit pointers in 16-bit task slots — see fix #26.
   - Pallet Town (`MAP_PALLET_TOWN`, map **3/0**, 24×20) renders its primary/secondary tilesets, weather, signs, and object events (sign lady at `(3,10)`, fat man at `(13,17)`, Oak at `(10,8)` hidden by `FLAG_HIDE_OAK_IN_PALLET_TOWN`). Its two connections (north `MAP_ROUTE1`, south `MAP_ROUTE21_NORTH`) are generated — see fix #30.
   - Verified with a scripted `--skip-intro` run (input injected through `REG_KEYINPUT` in `WaitForVBlank`, same path as SDL input): bedroom → stairs → 1F → front door → out to Pallet Town → north across the border into Route 1 (map **3/19**, 24×40) → south back into Pallet Town → back in through the front door to 1F. No crash; controls unlocked at every stage (asserted via `ArePlayerFieldControlsLocked()`/`ScriptContext_IsEnabled()` dumps); PPU captures checked for the exterior and Route 1. The temporary probe and scripted input were removed afterwards.
   - **Oak's interception trigger is Complete:** Pallet Town's coord events at `(12,1)`/`(13,1)` (`PalletTown_EventScript_OakTriggerLeft/Right`, `VAR_MAP_SCENE_PALLET_TOWN_OAK == 0`) now compile their real script closure natively. Attempting to walk north out of town triggers Oak's cutscene, walking sequence, lab door animation, and warp into Oak's Lab (see milestone and item 4 below).
     - **Wild encounters are ENABLED (fix #29 superseded).** `StandardWildEncounter()` no longer early-outs under `PORTABLE` and the `StartWildBattle()` stub is gone, so tall grass now starts a real battle. The rustle is still absent because field effects do not render (next bullet).
     - **Field effects render.** The tall-grass rustle plays when the player steps into grass, and the rest of the ported effects follow the same path (see the Field Effects milestone). Effects whose native is not linked yet still stay NULL and do nothing (fix #49).
   - **Pallet Town exterior interactions (completed):** every A-press target in the town now runs its real script.
     - **Signs:** `PalletTown_EventScript_PlayersHouseSign` ("{PLAYER}'s house"), `_RivalsHouseSign` ("{RIVAL}'s house"), `_TownSign` ("PALLET TOWN …"), `_TrainerTips` ("TRAINER TIPS …"), `_OaksLabSign` ("OAK POKéMON RESEARCH LAB").
     - **NPCs:** `PalletTown_EventScript_SignLady` (full original script: `lock`, `SIGN_LADY_READY`/`FLAG_TEMP_2` branches, `playse SE_PIN`, three `applymovement`s, `copyobjectxytoperm`) and `_FatMan` ("Technology is amazing! …").
     - **Generator (`tools/gen_map_data.py`) support added for this scene:** script commands `playse`, `copyobjectxytoperm`, `famechecker`, `delay`, `textcolor`, `signmsg`, `normalmsg`; `.equ` symbols from map `scripts.inc` (e.g. `SIGN_LADY_READY` → `VAR_TEMP_2`); movement labels declared next to their map scripts (not only in `data/scripts/movement.inc`); and movement mnemonics resolved through `asm/macros/movement.inc` to their `MOVEMENT_ACTION_*` constant instead of a hardcoded table.
     - **Two generator defects fixed on the way:** constant resolution stripped every `U` from an expression, so `MAX_TRAINERS_COUNT` became `MAX_TRAINERS_CONT` and *every* constant derived from `SYS_FLAGS` (`TRAINER_FLAGS_END`, all system flags, `FLAG_OPENED_START_MENU`, …) failed to resolve — only C integer suffixes are stripped now; and each `NATIVE_SCRIPT_ROOTS` entry is compiled independently, so an unsupported command reports the scene on stderr and leaves it on its dummy-script fallback instead of aborting the whole generator or wiring a half-compiled script.
     - **Native specials are index-aligned** with `data/specials.inc` (see fix #32). `famechecker` reaches `NativeSpecial_SetFlavorTextFlagFromSpecialVars` (index 371), which mirrors the fame checker save-data bookkeeping in `src/fame_checker.c`; `SetWalkingIntoSignVars` (368) and `DisableMsgBoxWalkaway` (369) call the real functions in `src/script.c`.
     - **Verified** with a temporary warp-stand-turn-press probe under `--skip-intro`: 7/7 targets rendered their own text into `gStringVar4` (asserted against the expected charmap bytes) with `gSpecialVar_LastTalked` 1/2 for the two NPCs, and PPU captures show each sign/NPC dialogue on screen. A one-frame direction press turns the player without stepping; holding the key long enough makes him walk onto the tile instead.
     - **Signs match FRLG's walk-into behavior** (see fix #33): holding the faced direction into a sign opens its text automatically — this is genuine FireRed/LeafGreen behavior, not a port bug, and Ruby/Sapphire/Emerald do NOT have it. A-press works as well. Both go through `TrySetUpWalkIntoSignpostScript`/`GetInteractedBackgroundEventScript`, which call `MsgSetSignpost()`, so the box draws with the signpost frame. Pushing a direction away closes the box via the real `EventScript_CancelMessageBox`; before fix #33 that symbol was a dummy and the box frame lingered over the world while the player walked off.
3. **Professor Oak's Lab interactions: Complete**
   - **Every reachable A-press target in `MAP_PALLET_TOWN_PROFESSOR_OAKS_LAB` runs its real script.** Previously all ten object events and the four background events carried `sDummyScript`, so pressing A on an aide, the rival, a starter ball, a PC or a sign did nothing at all.
   - **NPCs:** `PalletTown_ProfessorOaksLab_EventScript_Aide1`/`Aide2`/`Aide3` ("I study POKéMON as PROF. OAK's AIDE." with the `FLAG_SYS_GAME_CLEAR` fame-checker variant) and `_Rival` (the `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` 3/2/0 branches).
   - **Objects:** the three item balls (`_BulbasaurBall`, `_SquirtleBall`, `_CharmanderBall`) and the two dex units (`_Pokedex`, "It's like an encyclopedia, but the pages are blank.").
   - **Background events:** the two computer terminals (`_Computer`, "There's an e-mail message here. …") and the two signs (`_LeftSign` "PRESS START…", `_RightSign` "SAVE…" with its `VAR >= 6` alternate text).
   - **Oak (localId 4) now runs his real script, and the lab's map scripts run the starter scene.** `PalletTown_ProfessorOaksLab_EventScript_ProfOak` compiles natively (see item 5); the reachable branch on a fresh game is the `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB == 3` "your POKéMON can battle it" line. The dex/parcel branches above it are compiled too, but their triggers (delivering Oak's Parcel, `VAR_MAP_SCENE_VIRIDIAN_CITY_MART`) are still unreachable in the port.
   - **Generator (`tools/gen_map_data.py`) support added for this scene:** script commands `copyvar`, `waitstate`, `removeobject`, `showmonpic`, `hidemonpic`, `givemon`, `bufferspeciesname`, `MSGBOX_YESNO` (inlined as `message`/`waitmessage`/`yesnobox 20, 8` so it does not depend on the unemitted `gStdScripts` table) and the `YES`/`NO` comparison constants; shared text defined inline in `data/event_scripts.s` (e.g. `Text_GiveNicknameToThisMon`) is now a text source; and `parse_labels()` no longer treats an `.include` directive as a script command of whatever label precedes it.
   - **Smoke test:** `TestOverworldInteractions()` (`src/platform/main.c`) asserts all 10 lab object events and 4 BG events point at their real scripts, that the NPCs/balls/Oak begin with `lock`, that the BG events begin with `lockall`, that the first aide's message operand resolves through `gNativeScriptPtrs`, and that the map-script header tables (`ON_TRANSITION`/`ON_WARP_INTO_MAP_TABLE`/`ON_FRAME`) exist with the scene-1 frame entry resolving to `PalletTown_ProfessorOaksLab_ChooseStarterScene`.
   - **Verified** with a temporary warp-stand-turn-press probe under `--skip-intro`: 11/11 targets opened their own message (`gSpecialVar_LastTalked` 1/2/3/5/7/8/9 and charmap-correct `gStringVar4` bytes; the computer/PC and sign targets do not set `LastTalked`), each left `ScriptContext_IsEnabled()`/`ArePlayerFieldControlsLocked()` true while its box waited for a button press, and a PPU capture of the Aide1 box reads "I study POKéMON as PROF. OAK's AIDE.". The probe and injected input were removed afterwards.
4. **Professor Oak's Pallet Town Interception Cutscene: Complete**
   - **Every step of the Pallet Town exit interception cutscene is fully working.** Stepping onto `(12,1)` or `(13,1)` in Pallet Town on a fresh game (`VAR_MAP_SCENE_PALLET_TOWN_OAK == 0`) runs the real script closure. Oak stops the player, approaches from below, explains the danger of wild Pokémon without protection, escorts the player south through town to the lab, opens the door, enters, closes the door, and warps into `MAP_PALLET_TOWN_PROFESSOR_OAKS_LAB` at `(6,12)`.
   - `VAR_MAP_SCENE_PALLET_TOWN_OAK` becomes 1 (preventing re-triggering), `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` becomes 1, `FLAG_HIDE_OAK_IN_PALLET_TOWN` is set, and `FLAG_HIDE_OAK_IN_HIS_LAB` is cleared.

5. **Oak's Lab Starter Scene: Complete**
   - **Walking into the lab after the interception cutscene now runs the whole choose-a-starter flow with no extra input from the player beyond advancing dialogue.** The lab's `mapScripts` header is compiled natively (`ON_TRANSITION` → `ReadyOakForStarterScene`/`setflag FLAG_VISITED_OAKS_LAB`, `ON_WARP_INTO_MAP_TABLE` → `ReadyPlayerForStarterScene`/`turnobject LOCALID_PLAYER, DIR_NORTH`, `ON_FRAME_TABLE` scene-1 → `ChooseStarterScene`). `MapHeaderGetScriptTable`/`MapHeaderCheckScriptTable` (`src/script.c`) resolve those pointer operands through `gNativeScriptPtrs` on the host (fix #23).
   - **`ChooseStarterScene`** does the full original sequence: `lockall`, Oak walks in from the south entrance, `removeobject`/`setobjectxyperm`/`clearflag FLAG_HIDE_OAK_IN_HIS_LAB`, the player walks up to `(6,4)`, the rival walks in and waits, Oak's long multi-paragraph speech, `setvar VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB, 2`, `releaseall`.
   - **Starter selection:** each ball runs `showmonpic` + `MSGBOX_YESNO` (`ScriptMenu_ShowPokemonPic` in `src/script_menu.c`, now linked through `PREPROC_SRCS`, draws the species picture); `ChoseStarter` hides the picture, `removeobject VAR_LAST_TALKED`, sets `FLAG_SYS_POKEMON_GET`, `givemon PLAYER_STARTER_SPECIES, 5` (the real `ScriptGiveMon` in `src/script_pokemon_util.c`, no longer `PORTABLE`-guarded), copies `VAR_STARTER_MON`, plays the obtain fanfare, offers the nickname box (yes → `EventScript_ChangePokemonNickname` → `special ChangePokemonNickname`, mirrored in `src/platform/overworld_stubs.c`), then `RivalPicksStarter` walks the rival to the ball that counters the player's choice, removes it, and sets the scene var to 3.
   - **Oak is interactable** at every scene value; at scene 3 he answers "OAK: If a wild POKéMON appears, your POKéMON can battle it."
   - **Generator:** new `MapScriptRegistry` emits `map_script`/`map_script_2` header tables as native u8 arrays with `gNativeScriptPtrs` operands; new script commands `turnobject`, `savebgm`, `fadedefaultbgm`, `checkitem`, `additem`, `removeitem`, `compare`, `goto_if_questlog`, `giveitem_msg` (inlined fanfare/message/std-received-item sequence, since `gStdScripts` is not emitted), `map_script_2` and the `.2byte 0` sub-table terminator; `warp` with an explicit warp id now fails loudly instead of emitting a broken operand. `parse_labels()` also accepts trailing `@ comment` after a label.
   - **Runtime:** `src/script_menu.c` and `src/trainer_pokemon_sprites.c` are linked; `ScriptMenu_YesNo` is real, so the starter confirmation box works; `gSpecials` gained index-aligned `ChangePokemonNickname` (158), the quest-log wrappers, and the fossil-picture specials' prerequisites.
   - **Verified end-to-end on the real user path** (temporary probe: warp to Pallet Town `(12,4)`, walk north into the Oak trigger, then scripted A presses only): interception triggered at frame 170, cutscene warped into the lab at frame 1367 with scene var 1, `ChooseStarterScene` auto-ran and finished at frame 3138 (scene 2, controls unlocked), Bulbasaur received at frame 3590 (`species=1`, `VAR_STARTER_MON=0`, `FLAG_SYS_POKEMON_GET=1`, ball objects removed), rival took Charmander and the scene var reached 3 at frame 4614. PPU captures show the "This POKéMON is really quite energetic!" and "GREEN received the CHARMANDER from PROF. OAK!" boxes, plus an Oak A-press capture reading "OAK: If a w…". The probe was removed afterwards; `TestOverworldInteractions()` keeps the permanent wiring assertions.
   - **The starter scene's follow-on is Complete — see items 6 and 7.**

6. **Oak's Parcel Chain & Pokédex Hand-off: Complete**
   - **Walking into Viridian City's Mart now starts the parcel scene, and handing the parcel to Oak gives the Pokédex.** The Mart's `mapScripts` header (`ON_LOAD` -> `HideQuestionnaire`, `ON_FRAME_TABLE` -> `ParcelScene` while `VAR_MAP_SCENE_VIRIDIAN_CITY_MART == 0`) is compiled natively, so entering the Mart hands the player `ITEM_OAKS_PARCEL` and sets `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` to 5. Oak's own script then takes the `goto_if_ge VAR_MAP_SCENE_VIRIDIAN_CITY_MART, 1` branch into `ReceiveDexScene`.
   - **`ReceiveDexScene`** runs the full original sequence: the parcel is handed over (`removeitem ITEM_OAKS_PARCEL`), the rival walks in, Oak gives the Pokédex (`FLAG_SYS_POKEDEX_GET`, `special SetUnlockedPokedexFlags`), the two dex units on the desk are `removeobject`-ed, and five Poké Balls are added (`giveitem_msg ... ITEM_POKE_BALL, 5`). It ends by setting `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` to 6 and `VAR_MAP_SCENE_VIRIDIAN_CITY_MART` to 2.
   - **Route 1 and Viridian City are compiled too**, so the walk between Pallet Town and the Mart has real signs, NPC free-item scripts (`Route1_EventScript_MartClerk` gives a Potion) and the city's `ON_TRANSITION` (world-map flag + tutorial old man placement). The tutorial old man's battle triggers stay `NULL` (fix #27), still because `ViridianCity_EventScript_DoTutorialBattle` needs the unported `StartOldManTutorialBattle` special - the battle engine itself is now linked (see the Battle Engine milestone).
   - **Generator:** new `MapScriptRegistry` usage for a second map, plus `pokemart`, `setmetatile`, `checkitemspace`, `bufferitemname` and an accepted `.align` directive. `TextRegistry.reference_global()` emits engine-referenced text labels with external linkage: `tools/gen_map_data.py` scans linked `src/**.c` for `extern const u8 <label>[]` and emits those labels into the generated text bank, which is what lets `src/prof_pc.c`'s `PokedexRating_Text_*` rating messages resolve.
   - **Runtime:** `src/prof_pc.c` and `src/save_location.c` are linked, so `GetPokedexCount`, `GetProfOaksRatingMessage`, `SetUnlockedPokedexFlags` and `TrySetMapSaveWarpStatus` are the real implementations; the duplicate `TrySetMapSaveWarpStatus` stub was deleted and `gSpecials` gained index-aligned `GetPokedexCount` (212), `GetProfOaksRatingMessage` (213), `EnableNationalPokedex` (367), `SetUnlockedPokedexFlags` (385) and the quest-log/help-context entries (381-383, 391-392) the rating scripts need.
   - **`ScrCmd_pokemart` must not stop the script context under `PORTABLE`** — see fix #37. The clerk's shop branch now reports the gap and finishes its dialogue, so the field unlocks instead of freezing.
   - **Verified end-to-end** (temporary probe, since removed): entering the Mart ran the parcel scene at frame 782 (`VAR_MAP_SCENE_VIRIDIAN_CITY_MART` 1, `ITEM_OAKS_PARCEL` in the bag) and set the lab scene to 5 at frame 958; the A-press hand-over consumed the parcel, set `FLAG_SYS_POKEDEX_GET`, added five Poké Balls and moved the lab scene to 6 / Mart var to 2. PPU captures show Oak's dex-scene boxes ("So, I ...", "RED, I hate to say it,"). A second probe pass pressed A on the clerk with `pokemart` unported: the script printed `[Script] pokemart is not ported` and the player walked away with `locked=0`.
   - **Still to do after this scene:**
     - **Pokédex rating scenes** (`PokedexRating_EventScript_RateInPerson` and Oak's scene-6/9 branches): the specials they call (`GetPokedexCount`, `GetProfOaksRatingMessage`, `HasAllMons`, the help-context save/restore) are wired and compiled, but the scenes were not exercised. Oak's scene-6 branch also needs `RivalPicksStarter`-style movement coverage for the `VAR_FACING` variants.
     - **Shop UI:** `ScrCmd_pokemart` reports the gap because `src/shop.c` needs `src/item_menu.c` plus its INCBIN assets (`graphics/item_menu/list.bin`, `graphics/interface/select_button.4bpp`). Linking that pair — and adding `item_menu.c` to `PREPROC_SRCS` — would let the Mart clerk actually sell. **The BAG menu itself is now linked and working** (see item 8).
     - **Unwalked connection coverage:** the Viridian City side of Route 1's border was not walked (the verification warped to the Mart door because Route 1's ledges make scripted input unreliable). The Pallet Town side is covered by item 2.
     - **2F PC menu linkage** (`player_pc.c`, item storage).
     - **Field effects are ported for the effects whose natives are linked** — 57 of 70 scripts, including the whole grass family, dust, splashes, ripples, footprints and shadows. The 13 that stay NULL need `.c` files still unlinked: the five `FldEff_*Icon` emotes (`src/trainer_see.c`), `UseCutOnGrass`/`UseCutOnTree`/`CutGrass` (`src/fldeff_cut.c`), `UseDig` (`src/fldeff_dig.c`), `UseRockSmash` (`src/fldeff_rocksmash.c`), `UseStrength` (`src/fldeff_strength.c`), `UseTeleport` (`src/fldeff_teleport.c`) and `SweetScent` (`src/fldeff_sweetscent.c`). Link one, then re-run `python3 tools/gen_field_effect_data.py` — the scan picks it up and the effect starts working. See fix #49.

7. **Battle Engine: the full battle loop runs; the rival battle is wired end-to-end**
   - **The first rival battle now happens.** Leaving Oak's Lab after picking a starter used to walk straight over the scene-3 trigger tiles and out the door. Three separate gaps were closed:
     1. `PalletTown_ProfessorOaksLab_EventScript_RivalBattleTriggerLeft/Mid/Right` were not in `NATIVE_SCRIPT_ROOTS`, so `tools/gen_map_data.py` emitted `.script = NULL` for those three coord events and (per fix #27) a NULL coord-event script is simply walkable. The triggers (and their closure) are now compiled.
     2. `tools/gen_map_data.py` could not encode `trainerbattle_earlyrival`, so the whole rival closure failed and fell back to dummy scripts. It now emits `TRAINER_BATTLE_EARLY_RIVAL` (9) with the `RIVAL_BATTLE_*` flags operand, matching `sEarlyRivalBattleParams` in `battle_setup.c`.
     3. `EventScript_DoNoIntroTrainerBattle` (what `BattleSetup_ConfigureTrainerBattle` jumps into for `TRAINER_BATTLE_EARLY_RIVAL`) was a `{0x02}` stub in `battle_peripheral_stubs.c`, so the battle was configured and then instantly ended. It is now compiled from `data/scripts/trainer_battle.inc`, and the stub was deleted (fix #8).
   - **A move animation no longer hangs the game.** `tools/gen_battle_data.py` desynchronised every anim script through three macro-expander defects (fix #47), landing the interpreter on `Cmd_nop` — an empty body that never advances the script pointer. This affected *all* battles: the first move animation spun forever. `anim_data.h` was regenerated; `battle_data.h`/`ai_data.h` are unchanged.
   - **The battle loop is verified end-to-end** (both a direct probe and the real user path — Pallet Town → Oak's interception → lab → starter → walk down to `(7,8)`): the intro runs, FIGHT selection picks a move, damage and HP bars update (20/20 → 12/20), the opponent faints, exp is awarded, the player's mon levels up (Lv5 → Lv6, HP 10/22), the battle ends, and the field resumes with `locked=0`. Both outcomes work: a loss returns through `CB2_WhiteOut`'s heal path, and a win sets `VAR_MAP_SCENE_PALLET_TOWN_PROFESSOR_OAKS_LAB` to 4 via `EndRivalBattle`.
   - **`EndRivalBattle`** runs the real post-battle script: `special HealPlayerParty`, the rival's exit movement, `removeobject LOCALID_OAKS_LAB_RIVAL`, `setvar ... 4`, `setflag FLAG_BEAT_RIVAL_IN_OAKS_LAB`, `releaseall`. Because the scene var leaves 3, the trigger does not re-fire on the way out.
   - **Fixes required to make this reachable:** #46 (a wrong-typed `gCB2_AfterEvolution` stub faulted the first time a battle finished), #47 (anim macro expansion), #48 (trainerbattle speech operands are pointer-table indices).
   - **`gSpecials` gained** index-aligned `PlayTrainerEncounterMusic` (56) and `SetBattledTrainerFlag` (399) for the trainer-battle scripts.
   - **The battle's own animations render.** The field-to-battle transition (flashing intro + the main wipe the port's battles select: `B_TRANSITION_SLICE`/`WHITE_BARS_FADE` for wild, `POKEBALLS_TRAIL`/`ANGLED_WIPES` for trainer) plays, and the Poké Ball visibly spins as the player throws it — both were compositor gaps, not engine gaps (fix #51). Verified with a temporary probe (since removed): the real battle's send-out ball carries a rotating `gOamMatrices` entry (25°/frame on sprite 7, on-screen), and per-transition captures show each effect's wipe/scale progress rather than a plain fade. **The wild-battle transition used to tear** — a scrambled L-shaped band of background over a black field — because the HBlank-DMA replay advanced its destination through the I/O register block each scanline (fix #52). Re-captured after the fix, Slice renders its alternating scanline bands sliding out to converge, and Swirl/Wave/Clockwise-Wipe/Ripple/Spiral/Grid-Squares/Angled-Wipes/White-Bars-Fade/Blur all progress correctly; `TestOverworldInteractions()` asserts the destination stays pinned.
   - **Still unverified.** Trainer battles elsewhere (the six `EventScript_TryDo*` stubs in `battle_peripheral_stubs.c` are still `{0x02}`), Poké Ball catching, status/type-effect edge cases, and switching. Wild encounters reach the same engine and are covered by the same loop.
   - **Ordered remaining work.** Each item names what blocks it:
     1. **Shop UI (Mart clerk selling)** - `ScrCmd_pokemart` still reports `[Script] pokemart is not ported` (fix #37) because `src/shop.c`, `src/item_menu.c`'s shop locations and the `graphics/item_menu/list.bin` asset are not wired for selling. The BAG menu itself is linked and working (item 8).
     2. **Party menu** - `src/party_menu.c` is linked and the POKéMON submenu opens and renders correctly (item 8); switching and withdrawal have not been exercised.
     3. **Audio** - M4A was never linked (`src/platform/stubs.c`), so there are **no cries, no battle BGM and no sound effects**. Battles are silent.
     4. **`pokemon_special_anim`** - 12 stubs remain; blocks the level-up move-learning and TM animations.
     5. **Evolution scene** - separate scene, needed after level-up (note `gCB2_AfterEvolution` is a real writable variable now, so linking `src/evolution_scene.c` will not collide).
     6. **Ordinary trainer battles** - `gTrainers` data and the `EventScript_TryDo*` entry scripts are still stubs; the early-rival path does not use them.
     7. **PPU windows and HBlank DMA are now implemented** (see the Software PPU Compositor milestone): `REG_WIN0H`/`REG_WIN0V`/`REG_WININ`/`REG_WINOUT` are composited, `DmaSet` records HBlank-timed channels that the PPU replays per scanline, and affine OAM sprites rotate. Screens that mask via windows (party/status summaries) now composite through the real path instead of ignoring the window entirely; the OPTION menu's WIN0 cursor highlight is verified, the summaries themselves are not.
   - **Not needed for a wild or early-rival battle** (kept as stubs): Safari Zone (4), VS Seeker (3), Poké Dude tutorial (20), quest log (2), field moves (7).

8. **START menu, BAG, POKéMON and OPTION menus: Complete**
   - **Pressing START opens the real menu, and BAG / POKéMON / OPTION all open and render correctly.** The blocker was `SetTaskFuncWithFollowupFunc` truncating a 64-bit follow-up function pointer across two 16-bit task slots (fix #50) — `src/start_menu.c`, `src/item_menu.c` and `src/party_menu.c` all use that pattern, so every one of those screens jumped into unmapped memory the frame it tried to switch to its follow-up task.
   - **Linked for this scene:** `src/item_menu.c`, `src/bag.c`, `src/menu_indicators.c`, `src/start_menu.c`, `src/item_menu_icons.c`, `src/option_menu.c`, `src/save_menu_util.c` (the item/start-menu sources join `PREPROC_SRCS` because they carry `_()` strings and `INCBIN` assets). New asset rules: `bag_window_pal`, `select_button`, `red_arrow*`, `list.bin` and the menu icon sheets.
   - **`gNativeScriptPtrs` gained `EventScript_BagItemCanBeRegistered`** (`NATIVE_SCRIPT_ROOTS`), the message the BAG runs when an item is registered to SELECT; without it that path jumped into a NULL. Its text (`Text_BagItemCanBeRegistered`) is emitted into the generated text bank.
   - **Menu stubs deleted:** `src/platform/battle_engine_stubs.c` and `src/platform/battle_peripheral_stubs.c` dropped the `UseRegisteredKeyItemOnField`/start-menu/item-menu placeholders now provided by the real sources.
   - **Verified with a temporary probe** (`--post-rival`, since removed): START opened the menu; the BAG opened, listed `POTION x3`, `ANTIDOTE x2`, `POKé BALL x5` and `TM01` with correct descriptions; RIGHT switched from the ITEMS pocket to the next pocket (`gBagMenuState.pocket` 0 → 1); the POKéMON menu drew BULBASAUR Lv5 with its HP bar (19/19) and "Choose POKéMON or CANCEL."; the OPTION menu drew TEXT SPEED / BATTLE SCENE / BATTLE STYLE / SOUND / BUTTON MODE / FRAME / CANCEL. No crash, and the field resumes with `locked=0`.
   - **The OPTION menu's cursor highlight now renders.** It is a WIN0 band (`option_menu.c`'s `UpdateSettingSelectionDisplay` sets `REG_WIN0V`/`REG_WIN0H` to the selected row), so it was invisible until the compositor gained window support (fix #51). Verified with a temporary probe (since removed): `REG_WIN0V` tracks the cursor exactly — row 0 = `0x3A48` (y 58–72), row 3 = `0x616F` (y 97–111), 13 px per row — and a capture with 3 DOWNs shows the highlight on SOUND rather than TEXT SPEED.
   - **Still to do here:** actually selling from the Mart clerk (item 7.1), and exercising party switching/withdrawal (item 7.2).

## 7. Git & Commit Guidelines

- **One commit per modified file:** The project owner requests individual commits per modified file rather than omnibus commits.
- **Keep build artifacts ignored:** Do not commit `firered-native`, `*.bmp`, `*.png`, or IDE-specific `compile_flags.txt`. These are ignored in `.gitignore`.
